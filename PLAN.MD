# Current Engineering Plan

Regenerated on 2026-06-11 from the current `src/` and `tests/` implementation state. This file is the active roadmap; older historical status text has been removed.

## Implemented Baseline

- Runtime loader: MIPS PE parsing, import patching, runtime `LoadLibraryEx` mapping, dependency loading, forwarder resolution, trampoline patching, `DONT_RESOLVE_DLL_REFERENCES`, `LOAD_LIBRARY_AS_DATAFILE`, raw `LOAD_WITH_ALTERED_SEARCH_PATH` reuse, TLS directory parsing, CE-style process attach/detach refcount draining, thread attach/detach, and CE `DisableThreadLibraryCalls` filtering are implemented to the current test-backed level.
- Shell and file APIs: `ShellExecuteEx`, registry-backed file associations, shortcuts, recent-doc tracking, special folders, `SHGetFileInfo`, `ExtractIconExW`, resource-backed icon extraction, synthetic shell icons, image lists, selected DIB drawing, and icon rendering all have raw test coverage.
- Windowing and GWE: windows, message queues, default window proc behavior, modal menu/message paths, clipboard/caret support, accelerator translation, `SendMessageTimeout` blocking setup, timeout handling, and destroyed-target result writes are implemented to the current raw-test level.
- File system and storage: rooted host-backed paths, mount roots, directory enumeration, file attributes, change notification handles, `CeGetFileNotificationInfo`, duplicate coalescing, transient create/delete coalescing, and modified/delete coalescing are covered.
- Winsock and remote IO: CE-facing Winsock dispatch, isolated NAT addressing, TCP/UDP socket creation, bind/listen/connect/accept/send/recv/select paths, `FD_SETSIZE`/fd-set validation, nonblocking state, socket wait candidates, and scheduler wake integration are present.
- Core subsystems: thread handles, waits, events, semaphores, mutexes, TLS, critical sections, process handles, memory APIs, registry APIs, DPA/DSA containers including raw no-callback enum/destroy-callback edges, GDI object management, bitmap/DIB drawing, and scheduler readiness selection are established.

## Active Roadmap

1. Close the loader parity gaps outside the current happy path.
   - Add focused fixture variants for raw/non-Unicorn loader behavior where the Unicorn runtime has stronger coverage.
   - Keep validating dependency attach/detach ordering, forwarder failure modes, datafile loads, no-resolve loads, and invalid image edges.

2. Finish shell rendering parity.
   - Expand `ExtractIconExW` coverage across remaining PE icon formats, `RT_ICON` ordinal failure cases, and non-PE fallback behavior.
   - Add more `DrawIconEx` and image-list drawing cases for masks, additional stretched-size paths, framebuffer output, and invalid handle cleanup.

3. Finish modal UI and cross-thread message semantics.
   - Extend `SendMessageTimeout` beyond current timeout/destroyed-target cases into nested sends, reentrant waits, abort-if-hung edge cases, and `ReplyMessage` parity.
   - Keep menu and message-box behavior aligned with CE blocking, owner-window, and command-routing expectations.

4. Complete shell notification behavior.
   - Verify `Shell_NotifyIcon` and `SHNotification*` data lifetimes, update/remove paths, taskbar-visible state, and COM notification callback dispatch in integrated runtime scenarios.
   - Add negative tests for invalid callback pointers, stale handles, and process/thread ownership boundaries.

5. Harden mounted storage behavior.
   - Extend file-change notifications across mount roots, volume handles, rename churn, and FSDMGR-style ownership semantics.
   - Direct `fsdmgr.dll` path imports for create/remove directory, get/set attributes, delete, move, delete-and-rename, disk-free-space, find-first, create-file, system-file checks, and no-security-manager get/set file-security wrappers now route through mounted guest paths; remaining path/security gap is deeper ACL behavior.
   - Validate remaining live iNavi behavior against the mounted SD-card layout; raw writable vs read-only host-directory semantics and CE volume attributes are now covered.

6. Expand input, text, and GDI fidelity.
   - Add coverage for IME-facing keyboard input, caret timing, glyph metrics, font fallback, text clipping, palette selection, alpha/mask behavior, and device-context state restoration.

7. Advance the iNavi route/runtime frontier.
   - Continue from the current shell/process/window readiness point toward route search, destination entry, map rendering, and realistic navigation flow.
   - Continue from the validated detached remote-server listener/input path and rendered iNavi splash into the map UI transition.
   - Preserve traceability with targeted progress logs, source references, and reproducible command lines when behavior changes.

## Validation Gates

- Run `cargo fmt` after Rust edits.
- Run `cargo check --features unicorn,trace,win32-desktop` for shared subsystem changes.
- Run targeted tests for the touched area, for example:
  - `cargo test --features unicorn,trace,win32-desktop --test coredll_raw_kernel`
  - `cargo test --features unicorn,trace,win32-desktop --test coredll_raw_gwe`
  - `cargo test --features unicorn,trace,win32-desktop --test coredll_raw_memory_file`
  - `cargo test --features unicorn,trace,win32-desktop --test basic_subsystems`
- Run mounted-layout or application-level checks when touching process startup, shell routing, storage, or UI scheduling behavior.

## Completion Criteria

Delete this roadmap only after the active roadmap is empty, source/tests reflect the final state, and the validation gates pass for the completed scope.

## WinCE 6.0 Reference Gap Appendix

Reference tree used: `C:\WINCE600`, sampled on 2026-06-11. The local source already covers many APIs in these areas; the missing work below is the remaining semantic and validation delta against the CE 6.0 headers and private implementation patterns.

### Shell API And Notification Gaps

Reference files:
- `C:\WINCE600\PUBLIC\COMMON\SDK\INC\shellapi.h`
- `C:\WINCE600\PUBLIC\COMMON\SDK\INC\shellsdk.h`
- `C:\WINCE600\PUBLIC\WCESHELLFE\OAK\TASKMAN\minserver.cpp`
- `C:\WINCE600\PUBLIC\WCESHELLFE\OAK\TASKMAN\mintask.cpp`

Missing or under-validated:
- `Shell_NotifyIcon` now rejects duplicate `NIM_ADD` records, honors `NIF_MESSAGE`, `NIF_ICON`, `NIF_TIP`, and `NIF_STATE` when capturing `NOTIFYICONDATA` members, requires the fixed CE `NOTIFYICONDATAW` footprint with its 64-WCHAR `szTip` buffer before mutating state, follows the CE taskbar update rule that `NIM_MODIFY | NIF_ICON` with a null `hIcon` leaves the existing tray icon intact, routes successful updates to a registered taskbar by posting CE `WM_HANDLESHELLNOTIFYICON` with a copied `NOTIFYICONDATAW` payload, releases matching copied taskbar payloads after dispatch to match `mintask.cpp` ownership without freeing mismatched private payload types, verifies failed duplicate-add and missing-record modify calls do not post copied taskbar payloads, verifies live modify/delete calls still copy and post CE taskbar messages, preserves successful state updates without posting when the previously registered taskbar window has gone stale, and permits `NIM_DELETE` cleanup of an existing tray record after its owner HWND has gone stale while stale add/modify attempts still fail. Remaining gaps are taskbar-visible rendering/processing and broader tray-server routing validation against `minserver.cpp`, `mintask.cpp`, and the Explorer taskbar sources.
- `SHNotification*` now records integrated CE `IShellNotificationCallback` metadata for `OnCommandSelected`, `OnLinkSelected`, and `OnDismiss`, including vtable offsets and typed arguments, while leaving `SHNN_SHOW` as the CE taskbar `PopUp` window-notification-only path, and the raw `I` API no longer mistakes the marshalled `cbData` slot for a callback pointer. The Unicorn `GetMessageW` path now preserves non-null pending COM callback records when guest vtable dispatch cannot yet be entered, including unmapped callback-interface pointers, so transient dispatch failures do not silently drop CE bubble events. The OLE import stub now routes `CoInitialize*`/`CoUninitialize` through the local COM apartment tracker and `CoCreateInstance` through the local COM class registry by CE-style CLSID/IID GUID values, including `ppv` zeroing/writeback, aggregation rejection, and returning the registered local interface token when one exists. The shell notification callback queue now mirrors the CE taskbar `GetCallbackInterface` acquisition shape by requesting `IID_IShellNotificationCallback` from the local COM registry, storing the acquired interface token on queued link/dismiss/command callback records, and skipping COM callback records for unregistered CLSIDs while still delivering sink-window notifications. The Unicorn dispatcher now has a mapped guest interface-pointer regression that enters the vtable method with CE-style MIPS arguments and return bookkeeping. Remaining work is integrated Explorer/taskbar validation with a real guest COM object lifecycle and visual bubble rendering.
- `SHNotificationUpdate` now preserves existing icons on `SHNUM_ICON` updates with null `hicon`, records the replaced owned icon for destruction on non-null `SHNUM_ICON` updates like `UpdateBubble`, follows the CE update path by allowing stale `hwndSink` values in the incoming update struct while preserving the original sink, and moves records between inform and iconic notification lists when `SHNUM_PRIORITY` changes. Iconic notification expiration now uses CE's second-based `csDuration * 1000` taskbar timer scale, and expiration, explicit remove, and sink window/process cleanup record copied icon destruction like CE `UpdateTimedNotificationIcons`/`NIM_BUBBLE_DELETE` using `DeleteItem(..., TRUE)`. `SHNotificationRemove` and sink window/process cleanup now purge pending notification callback records and priority-list entries for the removed `(CLSID, id)`, `SHNotificationGetData` follows the CE fixed-title-buffer behavior when `cbTitle` is zero, `SHNotificationAddI` validates title/HTML presence by marshalled pointer nullness instead of string length, and overlong taskbar titles clear to an empty stored label like CE `StringCbCopy` failure into `CCHMAXTBLABEL`. Remaining gaps are broader title/HTML allocation failures and broader remove/update races.
- `SHGetFileInfo` now covers the CE-supported flag set shape for `SHGFI_ICON`, `SHGFI_DISPLAYNAME`, `SHGFI_TYPENAME`, `SHGFI_ATTRIBUTES`, `SHGFI_SYSICONINDEX`, `SHGFI_SMALLICON`, and `SHGFI_USEFILEATTRIBUTES`, including CE `SFGAO_*` shell-attribute output for filesystem, folder, shortcut, read-only, and inaccessible network-folder cases, CE `api.cpp` field-write behavior where `SHGFI_ICON` also fills `iIcon` and unrequested `hIcon`/`iIcon`/`dwAttributes` fields remain untouched, and distinct large/small system image-list handles for `SHGFI_ICON` and `SHGFI_SYSICONINDEX` queries. Synthetic system image-list handles now also accept `ImageList_Destroy` as a successful shell teardown path while keeping the shared emulator fallback reusable. Remaining gaps are broader shell-folder PIDL/display-name parity and more edge cases around icon-cache lifetime.
- `ExtractIconExW` now extracts PE resources, reports actual PE `RT_GROUP_ICON` counts for `nIconIndex == -1`, fills successive large/small output-array slots for multi-group extraction, selects distinct large/small `RT_ICON` entries from multi-size groups, covers string-named `RT_GROUP_ICON` entries with ordinal `RT_ICON` payloads, resolves sparse integer `RT_GROUP_ICON` resource IDs when zero-based enumeration does not cover the requested index, preserves indexed 1bpp, 4bpp, and 8bpp `RT_ICON` color tables, renders decoded 1bpp and 4bpp icon pixels, extracts and renders stride-padded 24bpp `BI_RGB` `RT_ICON` payloads, extracts and renders 32bpp `BI_RGB` `RT_ICON` BGRA pixels, applies extracted 24bpp AND-mask transparency during `DrawIconEx(DI_NORMAL)`, preserves and renders 16bpp `BI_BITFIELDS`/RGB555 `RT_ICON` masks, verifies `DestroyIcon` cleanup of PE-extracted owned color/mask backing, reports `ERROR_RESOURCE_NAME_NOT_FOUND` for malformed present group/icon resources and missing primary or secondary `RT_ICON` ordinals instead of falling back to synthetic shell icons, tolerates RT_ICON payloads that omit trailing AND-mask bytes by extracting a color-only icon, and preserves the synthetic one-icon fallback for non-PE shell-icon paths while leaving extra output slots and nonzero-index misses untouched. Raw `KernExtractIcons` now follows the CE `resource.cpp` datafile/group-resource path by copying selected `RT_ICON` payloads into guest heap memory for integer `RT_GROUP_ICON` resource IDs, extracts requested large/small outputs independently when one selected icon resource is missing, NULLs failed requested slots while preserving the peer success and last resource error, rejects zero-based group enumeration when the matching integer resource ID is absent without touching outputs, and reports `ERROR_RESOURCE_NAME_NOT_FOUND` when no large/small output pointer is supplied. Raw `GetFileVersionInfoSizeW` and `GetFileVersionInfoW` now extract integer `RT_VERSION/VS_VERSION_INFO` resources, validate `VS_FFI_SIGNATURE`, clear the size handle out-param, copy bounded version blocks, and rewrite copied `VERHEAD.wTotLen` like CE `resource.cpp`. Remaining gaps are broader PE format variants, live callback-based `KernExtractIcons` selection in Unicorn, and broader non-PE fallback edge cases.
- Raw `GetIconInfo` now distinguishes cursor and icon handles for CE `ICONINFO.fIcon` output, reports stock cursor handles as `FALSE` while preserving stock icon handles as `TRUE`, writes tracked `CreateIconIndirect`/bitmap-backed icon hotspot and mask/color bitmap fields back to the caller, returns the cloned `CreateIconIndirect` mask/color handles so caller-owned source bitmap deletion does not invalidate later icon drawing, exposes the rendered icon-owned mask/color bitmaps returned by bitmap-backed `ImageList_GetIcon`, verifies those returned bitmaps survive source `ImageList_Destroy` until `DestroyIcon`, and rejects invalid non-icon handles with `ERROR_INVALID_HANDLE`. Remaining gaps are live guest paths that consume returned mask/color handles outside the raw image-list bridge.
- `DrawIconEx` now paints bitmap-backed icons into framebuffers and selected memory DIBs, validates icon/HDC handles, scales framebuffer and selected-memory-DIB draws from the icon bitmap's native source dimensions when callers request a non-native destination size, preserves native bitmap-backed icon dimensions when callers pass zero width/height like CE `DrawIcon`/`ImageList::Add` paths, honors bitmap-backed `DI_MASK` by drawing the icon mask bitmap instead of the color image, and covers 1bpp mask-only framebuffer and selected-memory-DIB draws. `ImageList_Create` now rejects flags outside CE's `ILC_VALID` mask while still accepting CE-valid private/shared flags, and normalizes a zero CE color mask (`ILC_COLOR`) to `ILC_COLORDDB` before storing image-list flags. `ImageList_Add` and `ImageList_AddMasked` now reject bitmaps narrower than the image-list slot width instead of reporting success with zero appended strips, matching CE `ImageList::Add`; non-DDB image lists now latch the first indexed bitmap color table on add like CE's `GetDIBColorTable`/`m_bColorsSet` path and draw later indexed entries through that latched first palette, while DDB lists keep using each bitmap's own color table and later adds leave stored palette state untouched; raw `ImageList_AddMasked(CLR_DEFAULT)` and `ImageList_LoadImage(CLR_DEFAULT)` now sample the source bitmap's upper-left pixel as CE does through `ImageList::AddMasked`, raw masked add/load paths create CE-style mono mask backing and punch transparent source pixels to black, and real bitmap-backed add/replace paths snapshot source/mask pixels into owned image-list storage so deleting caller-owned bitmap handles does not invalidate later draws. `ImageList_LoadImage` now follows CE by substituting the loaded bitmap height when callers pass `cx == 0`, so square-height bitmap strips split correctly, failing negative `cx` values through the image-list creation gate, carrying CE mask/color-depth flags from `crMask` and the loaded bitmap into the created image list, and using the unmasked `ImageList_Add(..., NULL)` path when `crMask == CLR_NONE`. `ImageList_GetIconSize` now validates both output pointers before writing either dimension, matching CE's reviewed-parameter guard. `ImageList_GetIcon` now initializes its temporary icon mask bitmap to white like CE's `PatBlt(..., WHITENESS)` path, so unmasked image lists produce an all-white icon mask instead of deriving mask bits from the color image. `ImageList_ReplaceIcon` now accepts only `-1` as the append sentinel and rejects indexes below `-1`, matching CE's Win95-compatible guard; for real bitmap-backed icons it now mirrors CE `imagelist.cpp` by snapshotting rendered color and optional `DI_MASK` output into list-owned bitmap/mask storage while preserving pseudo-icon handle storage for synthetic shell icons. `ImageList_Merge` now sizes the merged list from the CE union of the two image rectangles and combines mask/color creation flags like CE. `ImageList_Copy` now rejects flags outside CE's `ILCF_VALID` mask, rejects CE-unsupported cross-list copies, copies only between valid slots in the same image list for `ILCF_MOVE`, and swaps same-list slots for `ILCF_SWAP`. `ImageList_Draw` now uses CE's `CLR_DEFAULT` background wrapper default, and `ImageList_DrawIndirect` now enforces the exact CE `IMAGELISTDRAWPARAMS` size before recording or rendering a draw, resolves `CLR_DEFAULT` backgrounds to the image-list background color, defaults zero `cx`/`cy` after applying `xBitmap`/`yBitmap`, writes those normalized draw parameters and CE's final overlay-pass `i`/`x`/`y`/`cx`/`cy`/`fStyle` mutations back into the caller's `IMAGELISTDRAWPARAMS`, recognizes CE's private `ILD_BLENDMASK` shape so `ILD_BLEND75` enters the bitmap-backed blend path instead of drawing unblended pixels, supports CE `rgbFg == CLR_NONE` destination blending for bitmap-backed color-image draw targets, follows CE's mask-only `rgbFg == CLR_NONE` blend branch by forcing `ILD_TRANSPARENT` and applying the 50% mono-dither/SRCAND mask path instead of destination tinting, applies CE `ILD_ROP`/`dwRop` raster operations for `ILD_MASK` and `ILD_IMAGE` selected-DIB/framebuffer draws, records CE `StretchBlt_I`-style DISPPERF rows for explicit `ILD_MASK`/`ILD_IMAGE` draw passes, and still draws registered overlays when `ILD_MASK` is combined with `ILD_OVERLAYMASK`. `ImageList_CopyDitherImage` now preserves destination image metadata like CE, masks `fStyle` to overlay bits, mutates bitmap-backed destination image pixels, and applies CE's 50% mono-dither/SRCAND mask update when destination and source mask storage is available. `ImageList_Duplicate` now deep-copies bitmap-backed image/mask storage like CE `CopyDIBBitmap`/`CopyBitmap` while preserving list metadata, overlays, and pseudo-handle entries. `ImageList_Remove`/`ImageList_SetImageCount` now preserve CE overlay slot indexes for single-image removals and truncation while only `ImageList_Remove(-1)` clears overlays and other negative remove indexes fail, and `ImageList_SetImageCount` now preserves the existing count and returns failure when the backing allocation request cannot be satisfied. `ImageList_SetOverlayImage` now rejects image lists created without `ILC_MASK` like CE's missing-mask-DC path, enforces CE's four overlay slots, computes CE-style overlay mask bounds/`ILD_IMAGE` metadata for drawing, covers CE's all-white-mask branch where the overlay record is zero-sized and paints no overlay pixels, preserves existing bounds when the same overlay slot is set to the same image like CE's early return, and keeps non-rectangular overlay masks mask-driven instead of promoting them to `ILD_IMAGE`; `ImageList_SetIconSize` now rejects no-op size changes while clearing all images/overlays on real size changes, including CE's changed zero/negative dimension case, `ImageList_BeginDrag` now creates the drag image hidden like CE's `s_DragContext.bDragShow = false` path, `ImageList_DragMove` now returns success even when no drag image is active while only moving visible drags, `ImageList_SetDragCursorImage` now treats a missing drag/dither image as a successful no-op, and `ImageList_GetDragImage`/`ImageList_DragEnter`/`ImageList_DragLeave` now preserve CE static drag point, hotspot, visibility, and lock state even when no drag image is active. Remaining gaps are broader mask-only format variants, broader indexed palette draw variants, additional overlay rendering edge cases, and additional cleanup/error combinations.

- `ImageList_Destroy` now releases image-list-owned bitmap/mask backing storage and heap allocations like CE `ImageList::Cleanup` deleting `m_hbmImage`/`m_hbmMask`; raw `ImageList_Replace`, `ImageList_ReplaceIcon`, `ImageList_Remove`, `ImageList_SetIconSize`, and `ImageList_SetImageCount` now also release cloned bitmap backing handles once no remaining list entry references them, while preserving shared strip handles that still have live entries. Raw `ImageList_SetBkColor` now applies CE `ResetBkColor`-style backing-pixel updates for bitmap-backed masked image lists, rewriting mask-on slots to black/white/the selected background while leaving mask-off source pixels unchanged. Raw `ImageList_GetIcon` now follows CE's mask/color draw pass for bitmap-backed entries with real backing storage by returning a real icon with rendered color and mask bitmaps instead of a bitmap-derived pseudo handle; local icon objects now track owned bitmap backing so `DestroyIcon` releases `ImageList_GetIcon`, PE-extracted, and copied `CreateIconIndirect` backing storage without deleting caller-owned source bitmaps. Synthetic shell/system image-list icons now ignore invalid overlay-mask slots above CE's four `ImageList_SetOverlayImage` entries for both returned pseudo handles and pseudo rendering, and draw their pseudo body/overlay markers into selected memory DIBs as well as framebuffers. Remaining cleanup/rendering work is narrower synthetic pseudo-icon rendering edge validation plus broader palette/dither cases.
- Direct `DrawIconEx` coverage now exercises shell/system `ImageList_GetIcon` pseudo handles with valid overlay flags and verifies invalid overlay slots above CE's four registered overlay entries remain ignored when those handles are drawn. Non-DDB image-list adds now latch the first indexed bitmap color table like CE and raw drawing verifies later indexed entries use that first palette, while `ILC_COLORDDB` lists draw each bitmap with its own palette. Remaining cleanup/rendering work is broader indexed palette draw cases, dither cases, and additional icon/image-list cleanup combinations.

### GWE, Menu, And Modal Message Gaps

Reference file:
- `C:\WINCE600\PUBLIC\COMMON\SDK\INC\winuser.h`

Missing or under-validated:
- `TrackPopupMenuEx` now covers the core CE popup loop shape, including `TPM_NONOTIFY`, `TPM_RETURNCMD`, disabled/default items, submenu cascades, owner notifications, cancellation behavior, CE horizontal/vertical alignment flags, top-level screen clamping against CE `SM_CXSCREEN`/`SM_CYSCREEN` menu size, top-level `TPMPARAMS.rcExclude` screen-rectangle avoidance with below-placement, right-side fallback after vertical candidates collide, left-side fallback when the right candidate clamps back into the excluded region, the final first-clamped-candidate fallback when every placement still intersects the exclusion, and cascade child-menu flip/clamp placement for left and bottom screen edges. Remaining gaps are broader live nested modal routing and additional unusual cancellation/edge-placement combinations.
- `MessageBoxW` now validates CE-supported button, default-button, icon, `MB_SETFOREGROUND`, `MB_TOPMOST`, and `MB_RTLREADING` flag shapes while rejecting unsupported desktop-only or undefined bits; it also covers default return IDs, yes/no/cancel layouts, owner-window disabled state, queued modal input, transient active-dialog recording, `MB_TOPMOST` creation with the CE `WS_EX_TOPMOST` extended style, and the shared GWE topmost z-order group used by `SetWindowPos(HWND_TOPMOST/HWND_NOTOPMOST)`. Remaining gaps are deeper modality, timeout behavior, foreground edge cases beyond activation/focus bookkeeping, and broader live user-driven dispatch.
- `SendMessageTimeout` currently covers important paths, including unsupported flag rejection, zero-timeout cross-thread expiry, Unicorn block-prep refusal for zero-timeout cross-thread sends so the raw expiry path owns that CE case, nonzero cross-thread queueing, nonzero timeout expiry without stale receiver delivery, CE-public `SMTO_NORMAL` nonzero cross-thread completion, destroyed-target completion, cross-thread `SMTO_ABORTIFHUNG` queuing just below the CE hung threshold, aborting once the receiver is hung, and aborting the combined `SMTO_BLOCK | SMTO_ABORTIFHUNG` path once the receiver is hung, same-thread synchronous dispatch/result writes, same-thread `SMTO_ABORTIFHUNG` dispatch despite a hung-threshold timestamp, early `ReplyMessage` result propagation, nested timeout sends where an inner `ReplyMessage` completes without clearing the outer send, and the CE `MessageTimeout::cReference`-style case where an active outer send times out while a nested send still dispatches and completes independently. Sent-message retrieval now honors caller HWND/min/max filters so queued notify sends and mount `WM_DEVICECHANGE` broadcasts do not satisfy unrelated `GetMessageW`/`PeekMessageW` filters. Runtime process handoff now clears stale cross-process send-yield snapshots when the corresponding live GWE sent-message record is gone, keeping remote/monitor rotation tied to the current CE send queue, and direct-send WNDPROC cleanup now preserves live callouts until the saved/debug PC reaches their return PC. Remaining gaps are broader reentrant dispatch, additional cross-thread `SMTO_BLOCK`/`SMTO_ABORTIFHUNG` edge combinations used by CE callers, and a fresh driven iNavi run to validate the repaired handoff in context.
- `MsgWaitForMultipleObjectsEx` now covers CE's ignored desktop `MWMO_WAITALL` bit, `QS_TIMER`, `QS_SENDMESSAGE`, `QS_ALLINPUT` over posted messages, paint, and timers, `MWMO_INPUTAVAILABLE` new-vs-existing input behavior, signaled-handle precedence over message wake, message-wake return indexing after multiple unsignaled handles, internal handle probing without double-counting the wait as a separate `WaitForMultipleObjects` scheduler attempt, unreadable handle-array failure with `ERROR_INVALID_PARAMETER` plus message-wait failure diagnostics, and CE invalid handle-array shapes for `nCount > MAXIMUM_WAIT_OBJECTS` or nonzero count with null `pHandles`. Remaining gaps are cancellation edges, additional live multi-handle waits, and live modal-loop interaction.

### File Notification And FSDMGR Gaps

Reference files:
- `C:\WINCE600\PRIVATE\WINCEOS\INC\fsnotify_api.h`
- `C:\WINCE600\PRIVATE\WINCEOS\COREOS\STORAGE\FSDMGR\pathapi.cpp`
- `C:\WINCE600\PRIVATE\WINCEOS\COREOS\STORAGE\FSDMGR\fsdmgrapi.cpp`
- `C:\WINCE600\PRIVATE\WINCEOS\COREOS\STORAGE\FSDMGR\fsdcache.cpp`
- `C:\WINCE600\PRIVATE\WINCEOS\COREOS\STORAGE\FSDMGR\nullcache.cpp`
- `C:\WINCE600\PRIVATE\WINCEOS\COREOS\STORAGE\FSDMGR\mounttable.cpp`
- `C:\WINCE600\PUBLIC\COMMON\OAK\INC\diskio.h`
- `C:\WINCE600\PRIVATE\WINCEOS\COREOS\STORAGE\NOTIFY\fsnotify.cpp`

Missing or under-validated:
- CE distinguishes external caller handles from internal/kernel-owned handles through `FSEXT_*`, `FSINT_*`, `NotifyGetNextChange`, and `INT_NotifyGetNextChange`; `FindCloseChangeNotification` now consumes valid-but-wrong handle types before reporting `ERROR_INVALID_HANDLE`, matching the public `DUPLICATE_CLOSE_SOURCE` path, while `FSINT_FindCloseChangeNotification` now rejects valid-but-wrong handles without consuming them like CE `INT_NotifyCloseChangeHandle`. Raw `DuplicateHandle` now validates source/target/options while creating independent local handle-table entries so duplicated notification handles survive `DUPLICATE_CLOSE_SOURCE`, duplicated notification handles are owned by the requested target process, public file-change notification handles now record the creating process and reject foreign-process wait/reset/info/duplicate/direct-`CloseHandle`/find-close attempts without consuming the handle, and direct `AFS_FindFirstChangeNotificationW` now honors its nonzero `hProc` owner, including foreign-caller direct `CloseHandle`/`DuplicateHandle` rejection without consuming the target-owned notification or publishing a duplicate. `fsdmgr.dll` imports now recognize the CE `fsdmgr.def` notification ordinals 68-75, paired `FSINT_*`/`FSEXT_*` names, `FSDMGR_AdvertiseInterface @2`, `FSDMGR_CacheIoControl @3`, `FSDMGR_CachedRead @4`, `FSDMGR_CachedWrite @5`, `FSDMGR_CreateCache @6`, `FSDMGR_CreateFileHandle @7`, `FSDMGR_CreateSearchHandle @8`, `FSDMGR_DeleteCache @9`, `FSDMGR_DeregisterVolume @10`, `FSDMGR_DiskIoControl @12`, `FSDMGR_FlushCache @14`, `FSDMGR_GetDiskInfo @16`, `FSDMGR_GetDiskName @17`, `FSDMGR_GetRegistryFlag @18`, `FSDMGR_GetRegistryString @19`, `FSDMGR_GetRegistryValue @20`, `FSDMGR_GetVolumeHandle @21`, `FSDMGR_GetVolumeName @22`, `FSDMGR_InvalidateCache @24`, `FSDMGR_ReadDisk @25`, `FSDMGR_ReadDiskEx @26`, `FSDMGR_RegisterVolume @27`, `FSDMGR_ResizeCache @30`, `FSDMGR_SyncCache @32`, `FSDMGR_WriteDisk @35`, `FSDMGR_WriteDiskEx @36`, `FSDMGR_GetMountFlags @37`, `STOREMGR_FsIoControlW @44`, `FSDMGR_AsyncEnterVolume @80`, `FSDMGR_AsyncExitVolume @81`, and `FSDMGR_ParseSecurityDescriptor @82`; `FSEXT_*` creates caller-owned first-change handles, while `FSINT_*` now creates internal FSDMGR-owned notification handles whose reset/info/close path works only through the paired internal imports and rejects public/`FSEXT` access without touching caller buffers. Raw AFS volume handles now record their owning process, reject foreign `AFS_Unmount` and `AFS_FsIoControlW`, close/unmount through `AFS_Unmount` or direct `CloseHandle`, consume the volume handle after the first close, signal root plus subpath file-change watchers through the mounted-root removal path, report mounted `CE_VOLUME_INFO` for `AFS_FsIoControlW(FSCTL_GET_VOLUME_INFO)` and the FSDMGR `STOREMGR_FsIoControlW` import, expose mounted volume folder names plus CE AFS hidden/system/permanent mount flags through FSDMGR import traps, map FSD `PDSK` tokens to HVOLs through the FSDMGR register/get-volume-handle support path, return FSD file/search context pointers through the CE FSDMGR create-handle shims, model the CE null-cache fallback ID table plus success/invalid/not-supported cache return surface, track direct `FSDMGR_AdvertiseInterface`/coredll `AdvertiseInterface` add/remove state by GUID/name with CE empty-name normalization, publish configured block-device `IClass` `\\StoreMgr\\<device>` advertisements with owner-scoped duplicate lifetime so one mounted root cannot remove another root's matching GUID/name advertisement, parse configured-mount `IClass` `GUID=name` entries plus `%d`, configured `%b`, and `%l` namespace names, cover coredll `EnumDeviceInterfaces @1874`, CE message queues (`CreateMsgQueue`/`OpenMsgQueue`/`ReadMsgQueue`/`WriteMsgQueue`/`GetMsgQueueInfo`/`CloseMsgQueue`) including access-direction, reader/writer wait readiness, explicit capacity, full-queue timeout, oversized-write, truncated-read, `MSGQUEUE_ALLOW_BROKEN` pipe-disconnect behavior, `MSGQUEUE_MSGALERT` priority/single-slot/capacity-counter behavior, and Unicorn blocking `ReadMsgQueue`/`WriteMsgQueue` timeout parking, and `RequestDeviceNotifications @1504`/`StopDeviceNotifications @1505` subscription lifecycle with message-queue `DEVDETAIL` attach/detach delivery, cover direct async enter/exit volume imports with registered-HVOL one-shot lock-token lifetime validation, cover CE security-attribute parsing for FSD create calls through direct `FSDMGR_ParseSecurityDescriptor`, cover FSDMGR registry-helper missing-value output clearing/status behavior plus configured logical-disk root/subkey lookup for DWORD/string/flag reads, cover `STOREMGR_FsIoControlW` refresh/flush no-ops plus host-backed unsupported-FSCTL no-touch failure, validate `FSCTL_COPY_EXTERNAL_START`/`COMPLETE` fixed `FILE_COPY_EXTERNAL` payloads before host-backed unsupported no-touch failure, cover file-handle `FSCTL_SET_FILE_CACHE(FileCacheDisableStandard)` no-op success plus unsupported enable/discard and volume-level rejection, cover public coredll `LockFileEx @1968`/`UnlockFileEx @1969` ordinal registration with CE wrapper-style readable-`OVERLAPPED` and file-handle validation plus host-backed byte-range shared/exclusive conflict state, exact owner/range unlock, and close-handle cleanup, and route direct/cache/Ex disk reads and writes through sparse synthetic sectors with CE `DISK_INFO`, `GETNAME`, `DEVICE_INFO`, `GET_STORAGEID`, `INITIALIZED`, `FORMAT_MEDIA`, `FORMAT_VOLUME`, `SCAN_VOLUME`, `GET_SECTOR_ADDR` validation with no-XIP unsupported failure, `COPY_EXTERNAL_START`/`COMPLETE` `DISK_COPY_EXTERNAL` payload validation with unsupported no-touch failure, `GETPMTIMINGS` synthetic zero-timing output, `SECURE_WIPE` sparse-sector clearing, `SET_SECURE_WIPE_FLAG` payload validation/no-op behavior, exact `DELETE_SECTOR_INFO` input-size rejection, `DELETE_SECTOR_INFO`-backed sector clearing, FMD XIP-mode, block-lock, sector-size, raw-write, reserved-read/write persistence plus `ReservedEntry` table output, region-table validation/storage, raw-block-size/info metadata, and flush-cache IOCTL results. Remaining work should validate physical block-driver backing, real device-manager `fsdev_t` interface handles beyond configured mount owner lifetime, real cache DLL/filter behavior beyond registry reads, real CE mounted-volume availability/powerdown/thread-wait reference behavior, broader file-security ACL get/set enforcement beyond descriptor parsing, CE-style file-lock blocking wait queues for non-immediate conflicts, remaining hardware-flash/callable FMD interface thunks/hardware-backed FLS discovery and specialized disk IOCTLs, real FATFS volume format/scan execution, real external-copy accelerator behavior, and real mounted-FSD `FsIoControl` hook forwarding beyond the host-backed unsupported stub.
- Direct `FSDMGR_DiskIoControl(IOCTL_DISK_FLUSH_CACHE)` is now named from CE `diskio.h` and covered as a successful no-op for synthetic sparse disks, matching the optional flush-forwarding behavior in `fsdcache.cpp` and `nullcache.cpp`.
- Direct `FSDMGR_GetDiskInfo @16` and `FSDMGR_GetDiskName @17` imports are now recognized from CE `fsdmgr.def`/`fsdmgrapi.cpp` and covered against the same synthetic disk metadata/name contract exposed through `FSDMGR_DiskIoControl`.
- Direct `FSDMGR_DiskIoControl(IOCTL_DISK_SETINFO)` now validates and persists the CE six-DWORD `DISK_INFO` payload for synthetic disks, and subsequent GETINFO calls report the updated metadata; real physical block-driver SETINFO forwarding remains queued.
- Direct `FSDMGR_DiskIoControl(IOCTL_DISK_FORMAT_VOLUME/IOCTL_DISK_SCAN_VOLUME)` now recognizes the CE FATFS volume controls from `diskio.h`; format-volume clears synthetic sparse sectors and scan-volume succeeds as a no-op, while real FATFS format/scan utility execution remains queued.
- Direct `FSDMGR_DiskIoControl(IOCTL_DISK_SET_STANDBY_TIMER/IOCTL_DISK_STANDBY_NOW/IOCTL_DISK_DELETE_CLUSTER/IOCTL_DISK_READ_CDROM/IOCTL_DISK_WRITE_CDROM)` now names these CE `diskio.h` specialized controls and covers the synthetic-disk unsupported/no-touch result; real standby power management, obsolete cluster deletion, and CD-ROM media IOCTL behavior remain queued behind actual block/CD drivers.
- Direct CE `FSDMGR_ReadDisk @25`/`FSDMGR_WriteDisk @35` now allow zero-sector requests with null caller buffers after validating the synthetic disk pointer, matching the `fsdmgrapi.cpp` wrapper shape that builds a one-entry SG request without a pre-buffer guard; real physical block-driver forwarding remains queued.
- Direct `FSDMGR_DiskIoControl(IOCTL_FMD_GET_INTERFACE)` now validates the CE 56-byte `FMDInterface` output buffer, writes the structure size, leaves the common wrapper-normalized `pBytesReturned` value at zero, and nulls unsupported callback slots until real callable FMD thunks exist.
- Direct `FSDMGR_DiskIoControl(IOCTL_FMD_SET_REGION_TABLE)` now follows the CE `fmd.h`/`fls.h` table shape by validating 28-byte `FlashRegion` entries, storing the per-disk synthetic table, returning the byte count written through `pBytesReturned`, reporting stored region and reserved-entry counts through `IOCTL_FMD_GET_INFO`, and refreshing a CE `FlashLayoutSector` image in synthetic sector zero; hardware flash forwarding and callable FMD callback behavior remain queued.
- Direct `FSDMGR_DiskIoControl(IOCTL_FMD_READ_RESERVED/WRITE_RESERVED/GET_RESERVED_TABLE)` now follows CE `fmd.h`/`fls.h` `ReservedReq` and `ReservedEntry` shapes for named synthetic reserved regions: reads are zero-filled before first write, writes persist byte ranges by name and offset, the table reports 16-byte entries with name/start/block-count metadata, and the synthetic FLS sector includes the reserved-entry byte count/table before the region table. Hardware-backed FLS discovery and hardware flash media behavior remain queued.
- File-handle `DeviceIoControl(IOCTL_FILE_READ_SCATTER/IOCTL_FILE_WRITE_GATHER)` now follows the CE `fileapi.cpp`/`filesystem.cpp` gate for non-null page-multiple `FILE_SEGMENT_ELEMENT` arrays and supports synchronous current-position plus reserved offset-array host-backed transfers; non-null `OVERLAPPED*` is accepted and ignored like cachefilt, while real lower-FSD/filter forwarding parity remains queued.
- Direct `FSDMGR_GetRegistryFlag @18`, `FSDMGR_GetRegistryString @19`, and `FSDMGR_GetRegistryValue @20` imports now follow the CE missing-value status/output-clearing surface from `fsdmgrapi.cpp`, `logicaldisk.cpp`, and `fsdhelper.cpp`, attach first-time FSD registrations to unused configured mounts, and read configured logical-disk root/subkey registry values for DWORD, string, and flag queries; real cache DLL/filter loading and utility execution remain queued.
- Direct `FSDMGR_DeviceHandleToHDSK @11` now follows the CE `storemain.cpp` identity-cast helper, and direct `FSDMGR_FormatVolume @15`/`FSDMGR_ScanVolume @31` now follow the CE `fsdmgrapi.cpp` `Util` registry probe: missing disk utility metadata returns `ERROR_FILE_NOT_FOUND`, while configured-but-not-loaded utility DLLs reach the current `ERROR_MOD_NOT_FOUND` execution boundary. Real disk-registry utility DLL loading/export execution remains queued.
- Direct `FSDMGR_AdvertiseInterface @2` now follows the CE wrapper path into coredll `AdvertiseInterface`, normalizes empty names to `\` like `fsdmgrapi.cpp`, and adds/removes tracked device-interface advertisements by GUID/name; configured mounted block devices now parse CE `IClass` GUID strings and explicit `GUID=name` entries, publish/remove `\StoreMgr\<device>`, custom-name, `%d`, configured `%b`, and `%l` advertisements at boot/unmount with owner-scoped duplicate lifetime, coredll `EnumDeviceInterfaces @1874` now enumerates the tracked advertisement table with CE GUID/name/size/error behavior, CE message queues now cover create/open/read/write/info/close, reader/writer wait readiness, read/write endpoint validation, explicit capacity, full-queue timeout, oversized-write, truncated-read, `MSGQUEUE_ALLOW_BROKEN` pipe-disconnect behavior, `MSGQUEUE_MSGALERT` priority/single-slot/capacity-counter behavior, and Unicorn blocking `ReadMsgQueue`/`WriteMsgQueue` timeout parking with CE-style replay on queue readiness or timeout, and coredll `RequestDeviceNotifications @1504`/`StopDeviceNotifications @1505` now create/cancel typed subscriptions and deliver `DEVDETAIL` attach/detach records through those queues. Real device-manager `fsdev_t` handles remain queued.
- Direct `FSDMGR_AsyncEnterVolume @80` and `FSDMGR_AsyncExitVolume @81` now follow the CE `fsdmgrapi.cpp` success/error status surface for registered HVOLs using a distinct one-shot HVOL lock token, including output-fault cleanup plus mismatched, duplicate, and stale exit rejection; real CE `MountedVolume_t::Enter/Exit` availability, powerdown, and thread-exit wait reference behavior remains queued.
- Direct `FSDMGR_ParseSecurityDescriptor @82` now follows the CE `fsdmgrapi.cpp`/`aclpriv.h` support-function surface for null `SECURITY_ATTRIBUTES`, validated kernel-mode descriptor pointers, and `SECDESHDR.cbSize` output. Coredll/AFS `GetFileSecurityW` and `SetFileSecurityW` now follow the CE `pathapi.cpp`/`fsdacl.h` no-security-manager shape by validating guest buffers, routing mounted paths, clearing `GetFileSecurityW` length-needed output to zero, and returning `ERROR_NOT_SUPPORTED` when no ACL DLL is active; broader ACL storage/enforcement remains queued.
- `FindFirstChangeNotificationW` now canonicalizes unrooted, `.`, and `..` watch paths before directory validation and watch registration, matching the CE `SafeGetCanonicalPathW` path into volume resolution, and preserves caller notification filter bits instead of rejecting unknown flags before the CE notify matcher gates on known bits. Root and non-root watches now honor the CE `WatchSubtree` boundary: non-recursive watches see only immediate children, while recursive watches see deeper descendants. `MoveFileW` file-change records now follow CE `NotifyMoveFileEx` boundaries, reporting same-parent moves as rename old/new and cross-parent moves as remove/add with file-vs-directory notify filters, and directory watchers now see self-rename/removal as CE current-directory removed (`"\\"`). Mounted change-notification handles now retain the owning mount root like CE `NOTVOLENTRY`-scoped events, so same child paths on different mounted volumes do not wake each other, same-parent mounted renames return old/new records only to the owning volume while recursive root watches see mounted-volume-prefixed old/new paths, cross-parent mounted renames return remove/add records to the owning volume while sibling mounted volumes stay quiet, and recursive root watchers still see mounted-volume-prefixed create/delete and rename paths. Remaining coverage should expand into less common notification lifetime/reset edges.
- `CeGetFileNotificationInfo` now follows the CE `FILE_NOTIFY_CHANGE_CEGETINFO` detail-record gate for signal-only vs detailed watches, follows CE `NotifyReset` buffer/reset behavior for prefix drains, re-signaling, no-more-items, undersized buffers, pending no-fit output-count write order, fitted-record count-pointer fault draining, partial caller-buffer fault copied-prefix draining, no-pending null-returned pointer ordering, the all-zero no-data reset path that consumes one outstanding signal, null buffers whose nonzero length reaches the guarded write path, no-pending guarded output writes with `ERROR_NO_MORE_ITEMS`, and trailing-NUL `FILE_NOTIFY_INFORMATION` record sizing/copying while preserving non-NUL `FileNameLength`, and `FindNextChangeNotification` now consumes one outstanding notification signal instead of clearing every queued detailed record. Successful writes still report `FILE_ACTION_MODIFIED`, and closing a changed file handle now reports CE's `FILE_ACTION_CHANGE_COMPLETED` detailed record with the FSDMGR `NotifyCloseHandle` filter mask. Access-denied read-only mount mutations now also leave matching watches unsignaled with no detailed records, and foreign-process notification handles now fail before draining records. Remaining gaps are broader error propagation and more exhaustive buffer/reset edge coverage.
- FSDMGR move behavior now covers mounted storage boundaries for cross-volume file moves, cross-volume directory rejection, mount-point rename errors, CE same-parent vs cross-parent notification action mapping, watched-directory self-rename/removal records, same-parent mounted rename scoping, and cross-parent mounted rename remove/add scoping across owning-volume, sibling-volume, and recursive-root change handles. `DeleteAndRenameFile`/`AFS_PrestoChangoFileName` now follows CE's delete-old/move-new direction, same-volume restriction, destination-preserving failure behavior, and delete-plus-move notification sequence. Cross-volume file moves now follow CE `FS_MoveFileW` by copying to the destination and treating source deletion as best-effort, so moves off read-only media can succeed while leaving the source file and only signaling the destination create. Mounted-root directory create/remove, file-delete, and attribute changes now follow CE `pathapi.cpp` errors, and read-only mounted host roots now report access-denied for raw create/copy/delete/directory/same-volume rename/attribute mutation attempts without mutating host storage or queuing change notifications. System, hidden, read-only, removable, and permanent mount metadata now follows CE `virtroot.cpp`, `pathapi.cpp`, `volumeapi.cpp`, `storemgr.h`, `fsdmgrapi.cpp`, and `pwindbas.h`: root enumeration skips hidden mount folders, exact hidden mount probes report `FILE_ATTRIBUTE_HIDDEN`, system mount roots and nested entries report `FILE_ATTRIBUTE_SYSTEM`, `CeGetVolumeInfoW`/`CeFsIoControlW(FSCTL_GET_VOLUME_INFO)`/volume-handle `AFS_FsIoControlW(FSCTL_GET_VOLUME_INFO)`/FSDMGR `STOREMGR_FsIoControlW(FSCTL_GET_VOLUME_INFO)` expose system/hidden/read-only/removable volume attributes, FSDMGR `GetVolumeName`/`GetMountFlags` import traps return mounted folder names plus AFS hidden/system/permanent flags, FSDMGR `RegisterVolume`/`GetVolumeHandle` import traps associate FSD disk pointers with registered HVOLs and follow CE mount-name suffix allocation through `Name9`, FSDMGR `CreateFileHandle`/`CreateSearchHandle` import traps return the FSD-owned context pointers as CE does, FSDMGR `STOREMGR_FsIoControlW` now covers refresh/flush no-op, copy-external fixed-payload validation/unsupported no-touch, and unsupported host-backed no-touch failure edges, file-handle `DeviceIoControl(FSCTL_SET_FILE_CACHE)` now follows the CE cache-filter disable-only surface, public coredll `LockFileEx`/`UnlockFileEx` now resolve, validate readable `OVERLAPPED*` plus file-handle inputs, and enforce host-backed byte-range shared/exclusive conflict state with exact owner/range unlock and close-handle cleanup, while FSDMGR cache import traps now follow the CE null-cache fallback for create/delete/resize/flush/sync/invalidate/ioctl status behavior while direct/cache/Ex sector reads and writes persist sparse synthetic disk sectors and `FSDMGR_DiskIoControl` handles CE `DISK_INFO` through legacy `DISK_IOCTL_GETINFO` and new `IOCTL_DISK_GETINFO`, metadata/status IOCTLs, SG read/write, `GET_SECTOR_ADDR` validation/no-XIP unsupported behavior, `COPY_EXTERNAL_START`/`COMPLETE` `DISK_COPY_EXTERNAL` payload validation/unsupported no-touch behavior, `GETPMTIMINGS` zero timing snapshots, secure-wipe sector clearing and set-flag validation/no-op behavior, `DELETE_SECTOR_INFO` sector clearing, format-media clearing, FMD XIP-mode, block-lock, sector-size, raw-write, reserved-read/write persistence plus `ReservedEntry` table output, region-table validation/storage, raw-block-size/info metadata, and flush-cache basics. Remaining volume-handle paths should be tested for physical block-driver backing, broader external cache DLL/filter behavior, CE-style file-lock blocking wait queues for non-immediate conflicts, remaining hardware-flash/callable FMD interface thunks/hardware-backed FLS discovery and specialized disk IOCTLs, real external-copy accelerator behavior, and real mounted-FSD `FsIoControl` hook forwarding beyond the host-backed unsupported stub.

### Loader And Runtime Gaps

Reference evidence:
- `C:\WINCE600\PRIVATE\WINCEOS\COREOS\NK\TOOLS\ROMIMAGE\ROMIMAGE\pbtimebomb.cpp`
- `C:\WINCE600\PRIVATE\WINCEOS\COREOS\NK\LOGGER\celoginstrumentation.c`
- `C:\WINCE600\PRIVATE\WINCEOS\COREOS\CORE\DLL\loader.cpp`
- `C:\WINCE600\PUBLIC\OSTEST\CESYSGEN\makefile`

Missing or under-validated:
- `LoadLibraryEx` now validates CE resource-only combinations such as `LOAD_LIBRARY_AS_DATAFILE | DONT_RESOLVE_DLL_REFERENCES`, rejects non-null `hFile` handles with `ERROR_INVALID_PARAMETER`, and promotes an already loaded `DONT_RESOLVE_DLL_REFERENCES` module to normal loaded-module flags when a later raw/normal load requests it without no-import flags. Keep expanding cases around resource-only handle behavior as more CE source consumers are found.
- Loader lifecycle now covers CE `ThreadNotifyDLLs`-style `DLL_THREAD_ATTACH`/`DLL_THREAD_DETACH` entrypoint callouts for imported modules that are not loading, no-import, or marked by `DisableThreadLibraryCalls`, and runtime `ProcessDetachAllDLLs` now follows CE `ProcessDetach` min-refcount ordering before releasing per-process module refs. Raw `ProcessDetachAllDLLs` cannot enter guest callouts, but it now drains eligible loaded-module refs while preserving no-import/datafile modules. Remaining lifecycle gaps are build-entry details around `DllEntry`, `_DllMainCRTStartup`, C++ static constructors/destructors, and broader process/thread ordering fixtures.
- Runtime DLL mapping parity should be audited outside Unicorn so raw loader tests and Unicorn execution do not silently diverge.
- Forwarder resolution now includes a host fixture where a named export forwards to a target DLL ordinal, and malformed literal forwarder strings fail closed instead of being whitespace-normalized into valid targets. Runtime `LoadLibraryW` failures now release modules newly loaded during the failed attempt, matching CE `DoImports` cleanup of dependency blocks, final dynamic `FreeLibrary` now releases the module's dependency refs after detach planning, failed runtime DLL maps now unmap their current image reservation before returning failure, current-image resource/trap/trampoline/module state is committed only after fallible parse/forwarder/resource steps succeed, and `DllMain(DLL_PROCESS_ATTACH)` false returns now set `ERROR_DLL_INIT_FAILED` while releasing refs owned by the failed load attempt. Remaining gaps are deeper lifecycle failure variants and broader integration fixtures for loader failure paths.
- `StringCompress`/`StringDecompress` now follow the CE `compr2.c` string-wrapper packet shape for all-zero input, raw even/odd byte streams when the raw packet is smaller than the original, and shrinkable nonzero streams stored as emulator-owned opaque payloads, including low-byte-only, high-byte-only, odd-length padded output, size-only query, compressed-half fail-closed, and CE failure return behavior for non-shrinking raw input. Remaining work is byte-for-byte parity with the real private `CECompress`/`CEDecompress` engine if its body becomes available; the reviewed CE source tree exposes its declarations/callers but not the engine body, so unknown non-raw external payloads still fail closed.

### Winsock And Network Gaps

Reference evidence:
- `C:\WINCE600\PRIVATE\TEST\NET\WINSOCK\22`
- `C:\WINCE600\PRIVATE\SERVERS\WEBPROXY\CORE\session.cpp`
- `C:\WINCE600\PRIVATE\WINCEOS\COREOS\DEVICE\SERVICES\CORE\servacpt.cpp`

Missing or under-validated:
- Local Winsock already has core socket calls, `ioctlsocket`, `setsockopt`, `getsockopt`, and `select`; `select` now accepts CE caller `nfds` values of `0`, `-1`, and active counts while rejecting null fd-set triads, `fd_count > FD_SETSIZE`, invalid socket handles, and fd-set memory faults with Winsock errors. Mixed read/write/except set filtering, zero-ready recovery, graceful TCP peer-close read readiness, TCP half-close read/write behavior, TCP reset read readiness/error propagation, repeated listener `select`/`accept` loops, and `SO_ERROR` clearing after `getsockopt` are covered. Remaining work is broader CE stress parity across higher-volume socket churn and full webproxy/service accept-loop patterns.
- Add more tests based on CE server/stress patterns for higher-volume socket churn, concurrent accept/send/close transitions, and webproxy/service session lifetimes.

### Input, IME, Text, And GDI Gaps

Reference evidence:
- `C:\WINCE600\PUBLIC\WCESHELLFE\OAK\INPUTMETHODS`
- `C:\WINCE600\PUBLIC\COMMON\SDK\SAMPLES\TESTIME`
- `C:\WINCE600\PUBLIC\COMMON\SDK\SAMPLES\MULTIBOX`
- `C:\WINCE600\PUBLIC\COMMON\SDK\SAMPLES\CHTIM`
- `C:\WINCE600\PRIVATE\TEST\STRESS\STRESS\MODULES\FONT`
- `C:\WINCE600\PRIVATE\TEST\STRESS\STRESS\MODULES\DIB`
- `C:\WINCE600\PRIVATE\TEST\STRESS\STRESS\MODULES\ALPHA`

Missing or under-validated:
- IME-facing input paths now cover `ImmGetOpenStatus(NULL)`, `ImmSetOpenStatus(NULL, ...)`, `ImmGetConversionStatus(NULL, ...)`, `ImmSetConversionStatus(NULL, ...)`, `ImmGetCompositionStringW(NULL, GCS_COMPSTR, ...)`, `ImmSetCompositionStringW(NULL, SCS_SETSTR, ...)`, `ImmGet/SetCompositionWindow`, `ImmGet/SetCompositionFontW`, `ImmGet/SetCandidateWindow`, `ImmGet/SetStatusWindowPos`, `RegisterSIPanel`, `SHSipPreferenceI`, `ImmSIPanelState(SIP_QUERY_LOCATION/SIP_SET_LOCATION/SIP_INPUT_ATTRIBUTES)`, `ImmGetDefaultIMEWnd`/`DefaultImeWndGet` focused-window and caller-HWND proxy behavior, `ImmIsUIMessageW` forwarding for the CE `WM_IME_*` UI message family, `ImmGetHotKey` no-registered-hotkey output clearing, `ImmGetGuideLineW` no-guideline level/string/private-data query behavior plus HIMCC-backed `GUIDELINE` level/index/string/private payload reads through `hGuideLine`, `ImmLockIMC`/`ImmUnlockIMC` and `ImmLockIMCC`/`ImmUnlockIMCC` guest-readable `INPUTCONTEXT`, `COMPOSITIONSTRING`, and `CANDIDATEINFO` buffers with lock-count/size queries, `ImmUnlockIMC` readback of resized `hCompStr`/`hCandInfo` `COMPOSITIONSTRING`/`CANDIDATEINFO` payload mutations, `ImmGetCandidateListCountW`/`ImmGetCandidateListW` CE `CANDIDATELIST` payload marshaling for stored HIMC candidate lists, CE-shaped `ImmNotifyIME` candidate open/change/close plus candidate selection/page, composition-font, and status-window notifications, TESTIME/`imm.h`-backed `ImmGetProperty` capability values, TESTIME resource-backed `ImmGetIMEFileNameW`/`ImmGetDescriptionW` strings for IME HKLs, TESTIME zero-result `ImmGetConversionListW`, TESTIME `ImmEscapeW(IME_ESC_QUERY_SUPPORT)` self-query behavior, CE keyboard-layout transition APIs including loaded HKL lists, `LoadKeyboardLayoutW` `KLF_ACTIVATE`, `ActivateKeyboardLayout` `HKL_NEXT`/`HKL_PREV` cycling, TESTIME `dic.c`-backed single-letter candidate generation for synthetic IME HKLs, TESTIME `regword.c` fake-word style/register/unregister state plus registered-word candidate inclusion and `ImmEnumRegisterWordW` callback enumeration, and TESTIME `GenerateMessage`/`hMsgBuf` `ImmGenerateMessage` queue flushing, resolving active-HIMC calls through the current foreground/focused keyboard target's HIMC where the CE input-method samples do so. Remaining coverage is still needed for rendered guideline/candidate UI callbacks, broader dictionary/private-profile candidate sources, and broader IME handoff.
- `MapVirtualKeyW` now follows the CE PC keyboard driver's scan-code mode split for modifiers: `MAPVK_VK_TO_VSC` returns the public low scan byte, `MAPVK_VSC_TO_VK` collapses left/right modifier VKs to common VKs, `MAPVK_VSC_TO_VK_EX` preserves side-specific VKs, and unsupported map types set `ERROR_INVALID_PARAMETER`. Broader keyboard-driver scan sequences and live IME/UI handoff remain open.
- TESTIME candidate-list generation now also rejects combined built-in/registered-word/private-profile lists above the CE `MAXCANDSTRNUM` ceiling of 32, matching `dic.c::ConvKanji`; broader rendered candidate UI and IME handoff remain open.
- TESTIME registered-word candidate lookup and Unicorn `ImmEnumRegisterWordW` callback enumeration now follow `stub.c::GetPrivateProfileString` by hiding private-profile entries for readings with lowercase characters while preserving uppercase readings.
- TESTIME candidate generation now also reads registry-value private-profile dictionary entries under `HKLM\SOFTWARE\Microsoft\testime\Windows\testime.DIC\<reading>` after the built-in table, preserving string data as the candidate text when present. Kernel boot now seeds the CE sample `testime.reg` dictionary into that active value-enumeration shape, including the numeric rolling candidates.
- Text and font behavior now includes selected-font `GetTextFaceW` parameter parity for null output buffers and negative counts, plus CE `GetTextExtentExPointW` invalid-parameter parity for null output `SIZE`, null input text with a positive count, and negative character counts. Device-context state coverage now includes CE `GetBkMode`, `SetBkMode`, `GetBkColor`, `SetBkColor`, `GetTextColor`, `SetTextColor`, `GetTextAlign`, and `SetTextAlign` invalid-HDC last-error behavior, `CLR_INVALID` background/text color round-trip error behavior, CE sentinel/validation behavior for `Get/SetStretchBltMode`, `Get/SetTextCharacterExtra`, and `Get/SetLayout`, CE invalid-HDC and previous-origin behavior for exported `SetViewportOrgEx`, CE `SetBrushOrgEx` invalid-HDC and previous-origin behavior, CE `SaveDC`/`RestoreDC` invalid-HDC, level-zero, negative-level, and absolute-level restoration behavior, and CE `GetDeviceCaps` handling for bad HDCs, null-primary HDCs, invalid `-1` indexes, `DRIVERVERSION`, unlimited brush/pen/marker/font counts, rectangular `CLIPCAPS`, square-pixel `ASPECT*`, `SIZEPALETTE`, and `NUMRESERVED`. Raw palette/object APIs now also cover CE `pal.cpp` invalid-handle/null-pointer/count-zero behavior for the tested entry APIs, `GetNearestColor` invalid-HDC behavior, non-paletted `GetSystemPaletteEntries` zero-result behavior on the RGB565 display, stock `DEFAULT_PALETTE` selection plus readable stock default-palette `GetObjectW`/`GetPaletteEntries`/nearest-index behavior, CE `pal.cpp` 256-entry user-palette create/set/get/nearest-index flow, `GetCurrentObject(OBJ_PAL)` selected/default-palette plus invalid-HDC/type behavior, `GetObjectType` CE `OBJ_DC`/`OBJ_MEMDC`/`OBJ_PAL` constants plus invalid-handle behavior, `GetStockObject(-1)` invalid-parameter behavior, and `SelectObject` invalid-HDC/null-object/bad-object behavior plus CE region-selection `SIMPLEREGION` return/status clipping. Raw `SelectClipRgn`/`GetClipRgn` now copies the selected region geometry into DC state like CE `clip.cpp::TestNormalClipRgn`, so callers may mutate or delete the source `HRGN` after selection, and `GetClipRgn(hdc, NULL)` is accepted only when no clip exists like `TestNoClipRgn`. Raw `GetTextExtentExPointW`, `DrawTextW`, and `ExtTextOutW` now apply CE `SetTextCharacterExtra` advance math for positive spacing, CE's first-character-preserving negative-spacing rule, caller-provided `ExtTextOutW` `lpDx` advances, and CE `WritableBitmapTest(EDrawTextW/EExtTextOut)` read-only selected-bitmap rejection, while selected CE Arial raster fonts now reject `SetTextCharacterExtra` without mutating the existing DC spacing value. Raw `GetCharABCWidthsI` now follows the CE `wingdi.h` glyph-index/count signature instead of the non-`I` first/last range shape, and plain 16px selected Tahoma `GetCharABCWidths`/`GetCharABCWidthsI` now use the CE GDIAPI `fontdata.h` ABC table for `!` through `z`. The stock/fallback `Tahoma` `TEXTMETRICW` path now carries the CE GDIAPI `fontdata.h` Tahoma `tmPitchAndFamily` and `tmCharSet` bytes, and plain 20px selected-font metrics now follow the CE GDIAPI `NTFontMetrics` rows for Tahoma, Courier New, Symbol, Times New Roman, Wingdings, and Verdana for the `TEXTMETRICW` fields the fixture compares. Plain 16px selected-font extents now use the CE GDIAPI `NTExtentResults` width tables for Tahoma, Courier New, Times New Roman, Wingdings, Verdana, and the Arial raster face across `GetTextExtentExPointW`, `GetCharWidth32`, and shared text-run measurement. Raw `DeleteObject` now preserves selected GDI object lifetime by failing while a font/brush/pen/bitmap/palette remains selected into any live DC, and selected-font metrics remain active until the DC selects another font. Raw `ExtTextOutW` also honors `OPAQUE` background mode for selected memory DIBs and framebuffers by filling text cells with the DC background color before drawing glyph pixels, clips glyph and opaque text-cell output through both the optional `ETO_CLIPPED` rectangle and the selected DC clip region, and validates null/bad HDCs before null text pointers to match CE `passNull2Text` last-error ordering. Broader CE-style glyph metrics outside the fixture ranges, additional font-size/style selection/fallback, complex text clipping, broader palette/device-color behavior, and additional font lifetime checks remain.
- Raw `IntersectClipRect`, `ExcludeClipRect`, and `GetClipBox` now follow CE `clip.cpp::passNull2ClipRegion` validation: null/bad HDCs fail with `ERROR_INVALID_HANDLE`, `GetClipBox(valid_hdc, NULL)` fails with `ERROR_INVALID_PARAMETER` before any output write, selected complex clip regions report `COMPLEXREGION` from `GetClipBox` like CE `clip.cpp::SelectComplexTest`, and clip-rect operations without a selected clip now start from the implicit HDC surface instead of an unbounded or empty clip.
- Raw `OffsetRgn` now returns the post-offset CE region status instead of collapsing every non-empty region to `SIMPLEREGION`: complex multi-rect regions keep `COMPLEXREGION`, null regions keep `NULLREGION`, and invalid handles still fail through the existing invalid-handle path.
- Raw `EqualRgn` now follows CE `region.cpp::passNull2Region(EEqualRgn)` by reporting `ERROR_INVALID_PARAMETER` for null operands, `ERROR_INVALID_HANDLE` for bad or wrong-type GDI handles, and clearing last error for valid equal/unequal region comparisons.
- Raw `PtInRegion` and `RectInRegion` now follow CE `region.cpp::passNull2Region(EPtInRegion/ERectInRegion)` by reporting `ERROR_INVALID_HANDLE` for null, bad, or wrong-type region handles, reporting `ERROR_INVALID_PARAMETER` for `RectInRegion(validRegion, NULL)`, and clearing last error for valid inside/outside tests.
- Raw `GetRgnBox` and `CombineRgn` now follow CE `region.cpp::passNull2Region(EGetRgnBox/ECombineRgn)` for the remaining parameter edges: `GetRgnBox(validRegion, NULL)` fails with `ERROR_INVALID_PARAMETER`, and invalid combine modes fail with `ERROR_INVALID_PARAMETER` instead of an unsupported-status path.
- Raw `GetRegionData` now follows CE `region.cpp::GetRegionDataCheckReturn` and `passNull2Region(EGetRegionData)` for size-query return counts, `RGNDATAHEADER`/RECT payload serialization, invalid region-handle errors, and caller-buffer write faults. `CreateRectRgnIndirect(NULL)` is now covered for the CE invalid-parameter path.
- Raw `CreateRectRgn`, `CreateRectRgnIndirect`, and `SetRectRgn` now cover CE `region.cpp::CreateNullRegionTest` and `SetRectRgnNULLRgnTest`: zero-width or zero-height inputs still create/update a valid region handle, but `GetRgnBox` reports `NULLREGION` with canonical `(0,0,0,0)` bounds.
- Raw zeroed `LOGFONTW` `CreateFontIndirectW` now follows CE `font.cpp::TestCreateFontIndirectZero`: after selection, `GetCurrentObject(OBJ_FONT)` returns the zero-font handle and `GetObjectW` returns an all-zero `LOGFONTW` instead of normalizing to fallback font fields.
- Raw `GetTextMetricsW` now follows CE `font.cpp::passOddSize` realized-height rows for plain known-font `CreateFontIndirectW` selections with `lfHeight` 0 and -24 across Tahoma, Courier New, Symbol, Times New Roman, and Wingdings.
- Raw `CreateFontIndirectW`/`GetObjectW` now preserves nonzero `LOGFONTW` escapement, orientation, precision, quality, pitch/family, style, charset, and face-name fields instead of dropping them during font object serialization.
- Raw stock-object lifetime now follows the CE `do.cpp::DeleteGetStockObjectTest` no-op success shape for stock brush, pen, font, and palette handles: `DeleteObject(GetStockObject(...))` reports success without destroying the reusable stock handle.
- Raw `GetStockObject` now recognizes the CE public `BORDERX_PEN` and `BORDERY_PEN` stock indexes as stable stock pens, including `GetObjectType`, `SelectObject`, and stock-delete no-op behavior.
- Raw `Polygon`/`Polyline` validation now follows CE `draw.cpp::passNull2Draw` and `SimplePolyTest`: null/bad HDCs fail with invalid-handle status, null point arrays fail with invalid-parameter status, `Polyline` rejects negative and single-point counts while accepting count zero as a successful no-op, and `Polygon` rejects counts below two while leaving last error at success. Raw selected-DIB `LineTo`, `Polyline`, and `Polygon` now also apply the DC viewport origin set by exported `SetViewportOrgEx` like CE `draw.cpp::ViewPort`, selected-DIB `Polygon`/`Polyline` drawing is covered against CE `draw.cpp::SimpleClipRgnTest0`-style clip-region containment, and two-point selected-DIB `Polygon` strokes now follow CE `ShapeColorTest(EPolygon)` by applying `R2_XORPEN` only once.
- Raw `Rectangle`/`Ellipse`/`RoundRect` HDC validation now follows CE `draw.cpp::passNull2Draw`: null and bad HDCs fail with `ERROR_INVALID_HANDLE` before any no-op drawing path. Raw selected-DIB shape draws now also apply the DC viewport/window origin set by exported `SetViewportOrgEx`, `OffsetViewportOrgEx`, and `SetWindowOrgEx` like CE `draw.cpp::ViewPort`, preserving the `RoundRect(..., 0, 0)` rectangle fallback through the shared rectangle path, and selected-DIB `Rectangle`/`RoundRect` outline pixels now cover CE `draw.cpp::ShapeColorTest` `R2_XORPEN` behavior. Raw `GetViewportOrgEx`, `GetWindowOrgEx`, `GetViewportExtEx`, and `GetWindowExtEx` are now exported at CE `core_common.def` ordinals `1988`, `1985`, `1989`, and `1986` and report selected-DIB origins/extents.
- Raw miscellaneous draw validation now follows CE `draw.cpp::passNull2Draw` for `RectVisible`, `FillRect`, `DrawFocusRect`, `DrawEdge`, `MoveToEx`, `LineTo`, `GetPixel`, `SetPixel`, `GetROP2`, `SetROP2`, `GetDIBColorTable`, `SetDIBColorTable`, `SetBitmapBits`, `PatBlt`, and `TransparentImage`: null/bad HDCs fail with invalid-handle status, valid-HDC null rectangles/color-table pointers fail with invalid-parameter status where CE checks them, invalid brushes and out-of-range selected-DIB color-table starts fail with invalid-handle status, `SetBitmapBits` now distinguishes null/bad bitmap handles from null/zero source payloads, allocates owned backing for pointerless `CreateBitmap(..., NULL)` handles before bounded byte copies like CE `CreateBitmapSquares*`/`SetBitmapBitsOnePixel`, and rejects loaded/resource-style read-only bitmap backing like CE `WritableBitmapTest`, `PatBlt` rejects CE-invalid ROP4 values with invalid-parameter status, rejects CE source-dependent ROP3 values with invalid-handle status like `PatBltBadRopTest`, renders source-free pattern/destination ROP3 values through the shared CE ROP3 truth table for selected DIBs and framebuffers, accepts CE `TryShapes`/`PatBltSimple` zero and negative signed extents as success/no-op or mirrored draws, and now rejects read-only selected bitmap destinations like CE `WritableBitmapTest(EBitBlt/EPatBlt/EStretchBlt/EMaskBlt/ETransparentImage/EGradientFill/EAlphaBlend/EStretchDIBits/ESetDIBitsToDevice/EDrawTextW/EExtTextOut)`, `FillRect`, `InvertRect`, `SetPixel`, `LineTo`, `Polyline`, `Polygon`, `Rectangle`, `Ellipse`, `RoundRect`, `DrawFocusRect`, and `DrawEdge` likewise reject read-only selected bitmap destinations like CE `WritableBitmapTest(EFillRect/EInvertRect/ESetPixel/ELineTo/EPolyline/EPolygon/ERectangle/EEllipse/ERoundRect/EDrawFocusRect/EDrawEdge)`, `DrawFocusRect` now XORs framebuffer HDC dotted perimeters and toggles back on a second draw like CE `DrawFocusRectTest`, `DrawEdge` rejects invalid edge-type bits on color display targets like CE `DrawEdgeTest1`, `DrawEdge` also fills a `BF_MIDDLE | BF_RECT` center with `COLOR_BTNFACE` and preserves a white center for `BF_FLAT | BF_RECT` like CE `DrawEdgeTest2/3`, `DrawEdge` `BF_ADJUST` now shrinks only the sides requested by the edge mask like CE public updown and WTL pane-title callers that use partial adjusted borders, and `DrawEdge` `BF_DIAGONAL_END*` now paints a bounded diagonal for CE tab/trackbar-style callers instead of succeeding as a no-op, `GetPixel` reads selected memory-DIB and framebuffer pixels instead of returning unconditional black, `TransparentImage` validates destination/source HDCs before dimensions like CE, rejects read-only selected bitmap destinations like CE `WritableBitmapTest(ETransparentImage)`, and now mirrors CE same-framebuffer black/white color-key rendering plus `TransparentBltErrorTest` near-miss color-key rejection, `RectVisible` preserves CE's `-1` error return, and `SetROP2(NULL, 0)` checks the handle before the invalid ROP2 value.
- CE `draw.cpp::TransparentBltTransparencyTest` now has raw coverage for its pointerless offscreen source pattern across the `CreateBitmap(..., NULL)` 1/2/4/8/16/24/32 bpp matrix: selecting the bitmap into a compatible memory DC allocates owned zeroed backing, `FillRect`/`SetPixel` paint into that storage, and `TransparentImage` reads it back through the source HDC for color-key copying. CE `draw.cpp::SimpleTransparentImageTest` translated-source compositing is covered for selected-DIB and framebuffer destinations, proving the green transparent source region preserves a black destination while the nested red rectangle copies from the offset source rectangle. CE `draw.cpp::BltAlphaDIBTest(ETransparentImage)` is now covered for top-down 32 bpp selected-DIB copies, preserving the source alpha byte in the copied destination DWORD when the transparent key does not match. CE `draw.cpp::TransparentImagePalTest(ETransparentImage)` is now covered for a 4 bpp selected-DIB source whose color table maps multiple palette indexes to the transparent RGB, using the CE-accepted hardware-driver outcome where all matching realized RGB entries preserve the framebuffer destination while nonmatching entries still copy. CE `draw.cpp::TransparentBltErrorTest` near-miss keys are covered for same-framebuffer HDC copies, and CE `draw.cpp::ClipBitBlt(ETransparentImage)` now has selected-DIB and framebuffer coverage for all four destination clipping edges, all four two-edge corner clipping combinations, fully clipped top-left/bottom-right no-op cases, one-pixel-visible top-left/bottom-right near-full clips, and the individual left/right/top/bottom fully clipped plus one-pixel-visible edge cases, proving left/top clipping advances the sampled source coordinate/row together while right/bottom clipping trims the visible tail, fully eliminated destinations return success without painting or dirtying the framebuffer, one-pixel survivors map to the expected source edge/corner and dirty only that pixel, and transparent pixels preserve the destination. Remaining work is the broader transparent-blit clipping/error matrix.
- Raw `GetObjectW` now follows CE `wingdi.h`/`do.cpp::GetObjectDIBTest` for `CreateDIBSection` handles: DIBSECTION-sized callers receive `BITMAP` plus `BITMAPINFOHEADER` metadata with the top-down sign preserved, while BITMAP-sized callers and non-DIBSECTION bitmaps still report the 24-byte `BITMAP` payload.
- Raw `CreateDIBSection` now also follows CE `draw.cpp::passNull2Draw` by rejecting bad nonzero HDC handles with `ERROR_INVALID_HANDLE`, while preserving null-HDC RGB DIB-section creation and null-HDC palette-index rejection.
- Raw `CreateDIBSection` now accepts CE public `wingdi.h` 12-byte `BITMAPCOREHEADER`/`BITMAPCOREINFO` inputs for indexed RGB DIB sections, using RGBTRIPLE color tables before allocating DIB backing.
- Raw `GradientFill` now follows CE's rectangle-only GPE surface: bad/null HDCs fail with invalid-handle status, null vertex/mesh pointers, zero counts, bad mesh indices, and `GRADIENT_FILL_TRIANGLE` fail with invalid-parameter status, read-only selected bitmap destinations are rejected like CE `WritableBitmapTest(EGradientFill)`, and `GetDeviceCaps(SHADEBLENDCAPS)` advertises alpha plus `SB_GRAD_RECT` without `SB_GRAD_TRI`.
- Raw `GetCharABCWidths` and `GetCharABCWidthsI` now follow CE `font.cpp::abcEscapementTest` by rejecting selected TrueType-style fonts with nonzero escapement and setting `ERROR_INVALID_PARAMETER`.
- Raw `ExtEscape` now follows the CE display-driver query/error surface for `QUERYESCSUPPORT`: valid display DCs report support for get/set gamma, get/set screen-rotation, `GETRAWFRAMEBUFFER`, and `DISPPERF_EXTESC_*` queries, reject invalid query buffers with `EXTESC_ERROR`/`ERROR_INVALID_PARAMETER`, reject the full CE GDIAPI `ExtEscapeInvalidAccess` private-display escape list with `EXTESC_ERROR`/`ERROR_INVALID_ACCESS`, implement CE `DRVESC_GETGAMMAVALUE`/`DRVESC_SETGAMMAVALUE` payloads with the `ddi_if.cpp` `cjIn` gamma-value and `pvIn` BOOL-buffer shape plus the `aablt.cpp` default/clamp range, implement CE VGAFLAT/SMI3DR `DRVESC_GETSCREENROTATION`/`DRVESC_SETSCREENROTATION` payloads with the packed supported-mask/current-mode value, `cjIn` rotation mode, and `DISP_CHANGE_BADMODE` rejection, implement the DeviceEmulator `gxdma.h` `GETRAWFRAMEBUFFER` `RawFrameBufferInfo` metadata payload with `FORMAT_565`, 16 bpp, `IMAGE_FRAMEBUFFER_UA_BASE`, byte/pitch strides, screen dimensions, and guest-readable RGB565 bytes behind the returned `pFramePointer`, and implement the CE-sized `DISPPERF_EXTESC_GETSIZE`, `GETTIMING`, `CLEARTIMING`, and `GETUNHANDLED` payload contract with local nonzero GPE timing rows for raw `BitBlt`, `StretchBlt`, `PatBlt`, non-copy `MaskBlt`, CE `DrvTransparentBlt`-style `TransparentImage`, explicit `ImageList_Draw*` `ILD_MASK`/`ILD_IMAGE` calls, direct-DIB `StretchDIBits`/`SetDIBitsToDevice` calls, and CE `DrvAlphaBlend` `0xCCCC` rows plus CE VGAFLAT `ROP_LINE` rows for raw `LineTo`/`Polyline`; raw `PatBlt` now also fills the CE `DispPerfParam` destination-video-memory and black/white solid-brush counters for display-target `PATCOPY` rows, same-framebuffer `TransparentImage` fills source-video-memory, destination-video-memory, and transparent counters, and framebuffer-destination `AlphaBlend` fills the destination-video-memory counter while preserving stretch accounting. Remaining work is full driver escape payload execution for video protection and broader DISPPERF coverage outside those raw draw paths.
- DeviceEmulator display `ExtEscape(SETBACKLIGHT/GETBACKLIGHT)` now follows `display_escapes.h` and `s3c2410x_lcd.cpp`: `SETBACKLIGHT` requires a four-byte BOOL input, `GETBACKLIGHT` requires a four-byte BOOL output, valid calls round-trip the local backlight state, and invalid sizes leave caller storage untouched. DeviceEmulator `ExtEscape(CONTRASTCOMMAND)` now follows the CE `pwingdi.h` `ContrastCmdInputParm` ABI plus `s3c2410x_lcd.cpp::ContrastControlHelper`: eight-byte command/parm input, optional four-byte output, get/set/increase/decrease/default/max command handling, signed 0..15 set-value clamping, CE's default-command zero return followed by max contrast on later get, and the shared LCDCON3 high-nibble coupling where contrast updates can change the bit that `GETBACKLIGHT` reports.
- Bitmap and alpha paths now include CE `BitBlt`/`StretchBlt` invalid-HDC and invalid ROP4 rejection, selected-DIB/framebuffer rendering for common CE ROP3 operations (`SRCPAINT`, `SRCAND`, `SRCINVERT`, `SRCERASE`, `MERGEPAINT`, `NOTSRCCOPY`, `NOTSRCERASE`, `DSTINVERT`), selected-brush pattern ROPs (`MERGECOPY`, `PATCOPY`, `PATINVERT`, `PATPAINT`), and generic CE ROP3 truth-table evaluation for the literal `draw.cpp::gnvRop3Array` cases across `BitBlt`/`StretchBlt`, CE `draw.cpp::NegativeSize` signed destination extent mirroring for selected-DIB source-copy `BitBlt` and null-mask `MaskBlt`, and CE `StretchBlt` signed destination/source extent mirroring for selected-DIB draws. Raw `TransparentImage` now also copies between framebuffer HDCs with the CE `TransparentBltPatBltTest` black/white color-key skip/copy behavior and accepts CE `NegativeSize`/`StretchBltFlipMirrorTest` signed destination/source extents for selected-DIB and framebuffer HDC copies. Raw `CreateDIBPatternBrushPt` now validates null/unsupported inputs like CE `brush.cpp`, creates a private bitmap from packed DIB data, and tiles through the existing pattern-brush renderer. Raw direct-DIB paths now render `StretchDIBits` and `SetDIBitsToDevice` into selected memory DIBs, decode CE GDIPRINT-style indexed 2 bpp `DIB_RGB_COLORS` caller DIBs, accept `DIB_PAL_COLORS` palette-index tables for direct indexed DIB sources, decode 8 bpp `BI_RLE8` and 4 bpp `BI_RLE4` compressed caller DIB bits when `biSizeImage` bounds the payload, validate CE bad-HDC/ROP4 edges for `StretchDIBits`, follow CE `SetDIBitsToDevice` null-DIB-payload invalid-parameter ordering before HDC validation, reject read-only selected bitmap destinations like CE `WritableBitmapTest(EStretchDIBits/ESetDIBitsToDevice)`, reject unsupported direct-DIB source bit depths before rendering, convert bottom-up `SetDIBitsToDevice` source scanline arguments like CE `SimpleSetDIBitsToDeviceTest`, accept CE's 24bpp `BI_BITFIELDS` `CreateDIBSection` quirk as plain BGR 24bpp while ignoring supplied masks, allow CE `CreateDIBSection` callers to pass a null `ppvBits` output pointer while still creating owned bitmap storage, back non-null `hSection` DIB bits with a file-mapping view that reports `dshSection`/`dsOffset`, seeds file-backed DIB-section bits from the mapped file, synchronizes through `FlushViewOfFile`, keeps multiple DIB-section views over the same mapping range coherent, now commits dirty section-backed DIB bytes back to the mapping, sibling views, and file-backed storage when flushed/deleted, and rejects unsupported bit depths, null/bad-HDC, oversized indexed RGB `biClrUsed`, or non-indexed/high-bpp `DIB_PAL_COLORS` `CreateDIBSection` requests like CE `verify.cpp::myCreateDIBSection` and `draw.cpp::CreateDIBSectionPalBadUsage`; raw `GetObjectW` now serializes CE-sized `DIBSECTION` metadata for `CreateDIBSection` handles when callers pass an 84-byte buffer. CE `MaskBlt` parameter validation for null/bad destination and source DCs, bad/color mask bitmaps, negative mask origins, and out-of-bounds 1bpp mask rectangles is covered, plus selected-DIB and framebuffer `MaskBlt` rendering for the CE `draw.cpp` `MAKEROP4(DSTCOPY, SRCCOPY)` mask-copy path, generic foreground/background ROP3-byte evaluation from CE `TestAllRops`-style ROP4 values, CE `swblt.cpp` negative destination extent mirroring for masked selected-DIB/framebuffer draws including framebuffer HDC negative destination-width coverage from CE `NegativeSize(EMaskBlt)`, and CE `ClipBitBlt(EMaskBlt)` all-edge clipping source/mask alignment for selected-DIB and framebuffer destinations. Raw `AlphaBlend` now follows CE invalid-HDC and blend-function validation for `BlendOp`, unsupported `AlphaFormat`, and 32bpp-only per-pixel alpha while accepting the CE GPE `BLT_ALPHASRCNEG`/`BLT_ALPHADESTNEG` blend flags, accepts zero source or destination dimensions as successful no-op rectangles like CE `AlphaBlendGoodRectTest`, rejects read-only selected bitmap destinations like CE `WritableBitmapTest(EAlphaBlend)` before source bitmap validation or blending, rejects negative dimensions and selected-DIB source rectangles outside the bitmap bounds like CE `AlphaBlendBadRectTest`, covers selected-DIB source-constant-alpha blending, applies CE premultiplied `AC_SRC_ALPHA` and non-premultiplied `AC_SRC_ALPHA_NONPREMULT` paths, applies CE `BLT_ALPHASRCNEG` source-alpha/source-constant inversion to selected-DIB and framebuffer destinations, applies CE `BLT_ALPHADESTNEG` destination-alpha inversion to 32bpp selected-DIB and alpha-capable framebuffer destinations, applies top-down and bottom-up 32bpp per-pixel alpha to selected-DIB destinations, applies source-constant plus top-down and bottom-up 32bpp per-pixel alpha to framebuffer destinations, clips negative selected-DIB and framebuffer destination origins while preserving CE stretch source mapping, and now uses CE GPE `swblt.cpp` Bresenham stretch sampling for uneven selected-DIB and framebuffer alpha stretches instead of simple floor-division sampling. Remaining gaps are broader DIB format/orientation variants, other DIB compression, remaining section-backed orientation/file-backed lifetime edge cases, broader palette/device-color behavior, additional TransparentBlt/MaskBlt ROP4 mirroring cases, and additional alpha orientation/clipping edge cases.
- CE `draw.cpp::StretchBltFlipMirrorTest(ETransparentImage)` negative source-width and source-height mirroring is now covered for selected-DIB and framebuffer destinations, proving horizontal and vertical source flipping through the color-key path without relying on a simultaneous destination flip.
- CE `draw.cpp::NegativeSize(EMaskBlt)` vertical and combined width/height destination mirroring is now covered for selected-DIB and framebuffer destinations with real 1 bpp masks, proving source and foreground/background ROP4 mask alignment through signed height and diagonal signed extents.
- Raw `CreateDIBSection` now accepts the CE alpha stress module's 16bpp 4-4-4-4 and 32bpp 8-8-8-8 `BI_ALPHABITFIELDS` DIB-section layouts with four masks, preserves the RGB masks plus the fourth alpha mask, skips the mask table before pixel storage, renders 16bpp alpha-bitfield sources through ordinary `BitBlt`, reports `BI_ALPHABITFIELDS` through `GetObjectW(DIBSECTION)`, and lets selected-DIB/framebuffer `AlphaBlend(AC_SRC_ALPHA)` consume 32bpp per-pixel alpha from the stored alpha mask instead of assuming high-byte alpha. Raw direct-DIB `StretchDIBits`/`SetDIBitsToDevice` now also decode CE GDIPRINT-style 16bpp BGR4444 and 32bpp BGR8888 `BI_ALPHABITFIELDS` masks. Broader alpha orientation/clipping variants remain open.
- AlphaBlend selected 32bpp per-pixel-alpha coverage now includes CE `alphablend.h::g_stcPPAlpha` rows 3, 38, 39, 40, and 75 from `draw.cpp::AlphaBlendPerPixelAlphaTest`, including the rounded premultiplied source scaling, destination attenuation, and saturating channel add behavior used by CE GPE `fastblt.cpp`.
- Section-backed DIB lifetime now also covers closing the caller's unnamed file-mapping handle while DIB views remain live: the closed handle is rejected for new mappings, but existing DIB views keep flushing/synchronizing until the last view is deleted.
- Direct CE FSDMGR file-lock imports are now part of the storage roadmap coverage: `FSDMGR_EmptyLockContainer @13`, `FSDMGR_InstallFileLock @23`, `FSDMGR_RemoveFileLock @28`, `FSDMGR_RemoveFileLockEx @29`, `FSDMGR_TestFileLock @33`, and `FSDMGR_TestFileLockEx @34` route through the existing host-backed range-lock state, covering readable `OVERLAPPED` install/remove, exact unlock, owner-wide unlock, current-position and explicit-offset lock probes, and peer conflict status by name and ordinal. Remaining file-lock parity is CE `FILELOCKSTATE` acquire/release callback forwarding, blocking wait queues/retry behavior for non-immediate conflicts, and lower-FSD/filter forwarding from `C:\WINCE600\PRIVATE\WINCEOS\COREOS\STORAGE\LOCKMGR\lockmgr.cpp` plus the public `lockmgr*.h` headers.
