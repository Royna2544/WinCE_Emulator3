# Current Engineering Plan

Regenerated on 2026-06-11 from the current `src/` and `tests/` implementation state. This file is the active roadmap; older historical status text has been removed.

## Implemented Baseline

- Runtime loader: MIPS PE parsing, import patching, runtime `LoadLibraryEx` mapping, dependency loading, forwarder resolution, trampoline patching, `DONT_RESOLVE_DLL_REFERENCES`, `LOAD_LIBRARY_AS_DATAFILE`, TLS directory parsing, CE-style process attach/detach refcount draining, thread attach/detach, and CE `DisableThreadLibraryCalls` filtering are implemented in the Unicorn path.
- Shell and file APIs: `ShellExecuteEx`, registry-backed file associations, shortcuts, recent-doc tracking, special folders, `SHGetFileInfo`, `ExtractIconExW`, resource-backed icon extraction, synthetic shell icons, image lists, selected DIB drawing, and icon rendering all have raw test coverage.
- Windowing and GWE: windows, message queues, default window proc behavior, modal menu/message paths, clipboard/caret support, accelerator translation, `SendMessageTimeout` blocking setup, timeout handling, and destroyed-target result writes are implemented to the current raw-test level.
- File system and storage: rooted host-backed paths, mount roots, directory enumeration, file attributes, change notification handles, `CeGetFileNotificationInfo`, duplicate coalescing, transient create/delete coalescing, and modified/delete coalescing are covered.
- Winsock and remote IO: CE-facing Winsock dispatch, isolated NAT addressing, TCP/UDP socket creation, bind/listen/connect/accept/send/recv/select paths, `FD_SETSIZE`/fd-set validation, nonblocking state, socket wait candidates, and scheduler wake integration are present.
- Core subsystems: thread handles, waits, events, semaphores, mutexes, TLS, critical sections, process handles, memory APIs, registry APIs, GDI object management, bitmap/DIB drawing, and scheduler readiness selection are established.

## Active Roadmap

1. Close the loader parity gaps outside the current happy path.
   - Add focused fixture variants for raw/non-Unicorn loader behavior where the Unicorn runtime has stronger coverage.
   - Keep validating dependency attach/detach ordering, forwarder failure modes, datafile loads, no-resolve loads, and invalid image edges.

2. Finish shell rendering parity.
   - Expand `ExtractIconExW` coverage across remaining PE icon formats, `RT_ICON` ordinal failure cases, and non-PE fallback behavior.
   - Add more `DrawIconEx` and image-list drawing cases for masks, additional stretched-size paths, framebuffer output, and invalid handle cleanup.

3. Finish modal UI and cross-thread message semantics.
   - Extend `SendMessageTimeout` beyond current timeout/destroyed-target cases into nested sends, reentrant waits, abort-if-hung edge cases, and `ReplyMessage` parity.
   - Keep menu and message-box behavior aligned with CE blocking, owner-window, and command-routing expectations.

4. Complete shell notification behavior.
   - Verify `Shell_NotifyIcon` and `SHNotification*` data lifetimes, update/remove paths, taskbar-visible state, and COM notification callback dispatch in integrated runtime scenarios.
   - Add negative tests for invalid callback pointers, stale handles, and process/thread ownership boundaries.

5. Harden mounted storage behavior.
   - Extend file-change notifications across mount roots, volume handles, rename churn, and FSDMGR-style ownership semantics.
   - Validate remaining live iNavi behavior against the mounted SD-card layout; raw writable vs read-only host-directory semantics and CE volume attributes are now covered.

6. Expand input, text, and GDI fidelity.
   - Add coverage for IME-facing keyboard input, caret timing, glyph metrics, font fallback, text clipping, palette selection, alpha/mask behavior, and device-context state restoration.

7. Advance the iNavi route/runtime frontier.
   - Continue from the current shell/process/window readiness point toward route search, destination entry, map rendering, and realistic navigation flow.
   - Preserve traceability with targeted progress logs, source references, and reproducible command lines when behavior changes.

## Validation Gates

- Run `cargo fmt` after Rust edits.
- Run `cargo check --features unicorn,trace,win32-desktop` for shared subsystem changes.
- Run targeted tests for the touched area, for example:
  - `cargo test --features unicorn,trace,win32-desktop --test coredll_raw_kernel`
  - `cargo test --features unicorn,trace,win32-desktop --test coredll_raw_gwe`
  - `cargo test --features unicorn,trace,win32-desktop --test coredll_raw_memory_file`
  - `cargo test --features unicorn,trace,win32-desktop --test basic_subsystems`
- Run mounted-layout or application-level checks when touching process startup, shell routing, storage, or UI scheduling behavior.

## Completion Criteria

Delete this roadmap only after the active roadmap is empty, source/tests reflect the final state, and the validation gates pass for the completed scope.

## WinCE 6.0 Reference Gap Appendix

Reference tree used: `C:\WINCE600`, sampled on 2026-06-11. The local source already covers many APIs in these areas; the missing work below is the remaining semantic and validation delta against the CE 6.0 headers and private implementation patterns.

### Shell API And Notification Gaps

Reference files:
- `C:\WINCE600\PUBLIC\COMMON\SDK\INC\shellapi.h`
- `C:\WINCE600\PUBLIC\COMMON\SDK\INC\shellsdk.h`
- `C:\WINCE600\PUBLIC\WCESHELLFE\OAK\TASKMAN\minserver.cpp`
- `C:\WINCE600\PUBLIC\WCESHELLFE\OAK\TASKMAN\mintask.cpp`

Missing or under-validated:
- `Shell_NotifyIcon` now rejects duplicate `NIM_ADD` records, honors `NIF_MESSAGE`, `NIF_ICON`, `NIF_TIP`, and `NIF_STATE` when capturing `NOTIFYICONDATA` members, requires the fixed CE `NOTIFYICONDATAW` footprint with its 64-WCHAR `szTip` buffer before mutating state, follows the CE taskbar update rule that `NIM_MODIFY | NIF_ICON` with a null `hIcon` leaves the existing tray icon intact, routes successful updates to a registered taskbar by posting CE `WM_HANDLESHELLNOTIFYICON` with a copied `NOTIFYICONDATAW` payload, releases matching copied taskbar payloads after dispatch to match `mintask.cpp` ownership without freeing mismatched private payload types, and preserves successful state updates without posting when the previously registered taskbar window has gone stale. Remaining gaps are taskbar-visible rendering/processing and broader tray-server routing validation against `minserver.cpp`, `mintask.cpp`, and the Explorer taskbar sources.
- `SHNotification*` now records integrated CE `IShellNotificationCallback` metadata for `OnCommandSelected`, `OnLinkSelected`, and `OnDismiss`, including vtable offsets and typed arguments, while leaving `SHNN_SHOW` as the CE taskbar `PopUp` window-notification-only path, and the raw `I` API no longer mistakes the marshalled `cbData` slot for a callback pointer. The Unicorn `GetMessageW` path now preserves non-null pending COM callback records when guest vtable dispatch cannot yet be entered, including unmapped callback-interface pointers, so transient dispatch failures do not silently drop CE bubble events. The OLE import stub now routes `CoInitialize*`/`CoUninitialize` through the local COM apartment tracker and `CoCreateInstance` through the local COM class registry by CE-style CLSID/IID GUID values, including `ppv` zeroing/writeback, aggregation rejection, and returning the registered local interface token when one exists. The shell notification callback queue now mirrors the CE taskbar `GetCallbackInterface` acquisition shape by requesting `IID_IShellNotificationCallback` from the local COM registry, storing the acquired interface token on queued link/dismiss/command callback records, and skipping COM callback records for unregistered CLSIDs while still delivering sink-window notifications. The Unicorn dispatcher now has a mapped guest interface-pointer regression that enters the vtable method with CE-style MIPS arguments and return bookkeeping. Remaining work is integrated Explorer/taskbar validation with a real guest COM object lifecycle and visual bubble rendering.
- `SHNotificationUpdate` now preserves existing icons on `SHNUM_ICON` updates with null `hicon`, records the replaced owned icon for destruction on non-null `SHNUM_ICON` updates like `UpdateBubble`, follows the CE update path by allowing stale `hwndSink` values in the incoming update struct while preserving the original sink, and moves records between inform and iconic notification lists when `SHNUM_PRIORITY` changes. Iconic notification expiration, explicit remove, and sink window/process cleanup now record copied icon destruction like CE `UpdateTimedNotificationIcons`/`NIM_BUBBLE_DELETE` using `DeleteItem(..., TRUE)`. `SHNotificationRemove` and sink window/process cleanup now purge pending notification callback records and priority-list entries for the removed `(CLSID, id)`, `SHNotificationGetData` follows the CE fixed-title-buffer behavior when `cbTitle` is zero, `SHNotificationAddI` validates title/HTML presence by marshalled pointer nullness instead of string length, and overlong taskbar titles clear to an empty stored label like CE `StringCbCopy` failure into `CCHMAXTBLABEL`. Remaining gaps are broader title/HTML allocation failures and broader remove/update races.
- `SHGetFileInfo` now covers the CE-supported flag set shape for `SHGFI_ICON`, `SHGFI_DISPLAYNAME`, `SHGFI_TYPENAME`, `SHGFI_ATTRIBUTES`, `SHGFI_SYSICONINDEX`, `SHGFI_SMALLICON`, and `SHGFI_USEFILEATTRIBUTES`, including CE `SFGAO_*` shell-attribute output for filesystem, folder, shortcut, read-only, and inaccessible network-folder cases, and returns the system image-list handle for both `SHGFI_ICON` and `SHGFI_SYSICONINDEX` queries. Remaining gaps are broader shell-folder PIDL/display-name parity and more edge cases around icon-cache lifetime.
- `ExtractIconExW` now extracts PE resources, reports actual PE `RT_GROUP_ICON` counts for `nIconIndex == -1`, fills successive large/small output-array slots for multi-group extraction, selects distinct large/small `RT_ICON` entries from multi-size groups, covers string-named `RT_GROUP_ICON` entries with ordinal `RT_ICON` payloads, resolves sparse integer `RT_GROUP_ICON` resource IDs when zero-based enumeration does not cover the requested index, preserves indexed 1bpp, 4bpp, and 8bpp `RT_ICON` color tables, renders decoded 1bpp and 4bpp icon pixels, extracts and renders stride-padded 24bpp `BI_RGB` `RT_ICON` payloads, applies extracted 24bpp AND-mask transparency during `DrawIconEx(DI_NORMAL)`, preserves and renders 16bpp `BI_BITFIELDS`/RGB555 `RT_ICON` masks, verifies `DestroyIcon` cleanup of PE-extracted owned color/mask backing, reports `ERROR_RESOURCE_NAME_NOT_FOUND` for malformed present group/icon resources and missing primary or secondary `RT_ICON` ordinals instead of falling back to synthetic shell icons, tolerates RT_ICON payloads that omit trailing AND-mask bytes by extracting a color-only icon, and preserves the synthetic one-icon fallback for non-PE shell-icon paths while leaving extra output slots and nonzero-index misses untouched. Raw `KernExtractIcons` now follows the CE `resource.cpp` datafile/group-resource path by copying selected `RT_ICON` payloads into guest heap memory for integer `RT_GROUP_ICON` resource IDs, rejects zero-based group enumeration when the matching integer resource ID is absent, and reports `ERROR_RESOURCE_NAME_NOT_FOUND` when no large/small output pointer is supplied. Remaining gaps are broader PE format variants, live callback-based `KernExtractIcons` selection in Unicorn, and broader non-PE fallback edge cases.
- `DrawIconEx` now paints bitmap-backed icons into framebuffers and selected memory DIBs, validates icon/HDC handles, scales framebuffer and selected-memory-DIB draws from the icon bitmap's native source dimensions when callers request a non-native destination size, honors bitmap-backed `DI_MASK` by drawing the icon mask bitmap instead of the color image, and covers 1bpp mask-only framebuffer and selected-memory-DIB draws. `ImageList_Create` now rejects flags outside CE's `ILC_VALID` mask while still accepting CE-valid private/shared flags, and normalizes a zero CE color mask (`ILC_COLOR`) to `ILC_COLORDDB` before storing image-list flags. `ImageList_Add` and `ImageList_AddMasked` now reject bitmaps narrower than the image-list slot width instead of reporting success with zero appended strips, matching CE `ImageList::Add`; raw `ImageList_AddMasked(CLR_DEFAULT)` and `ImageList_LoadImage(CLR_DEFAULT)` now sample the source bitmap's upper-left pixel as CE does through `ImageList::AddMasked`, raw masked add/load paths create CE-style mono mask backing and punch transparent source pixels to black, and real bitmap-backed add/replace paths snapshot source/mask pixels into owned image-list storage so deleting caller-owned bitmap handles does not invalidate later draws. `ImageList_LoadImage` now follows CE by substituting the loaded bitmap height when callers pass `cx == 0`, so square-height bitmap strips split correctly, failing negative `cx` values through the image-list creation gate, carrying CE mask/color-depth flags from `crMask` and the loaded bitmap into the created image list, and using the unmasked `ImageList_Add(..., NULL)` path when `crMask == CLR_NONE`. `ImageList_GetIconSize` now validates both output pointers before writing either dimension, matching CE's reviewed-parameter guard. `ImageList_ReplaceIcon` now accepts only `-1` as the append sentinel and rejects indexes below `-1`, matching CE's Win95-compatible guard. `ImageList_Merge` now sizes the merged list from the CE union of the two image rectangles and combines mask/color creation flags like CE. `ImageList_Copy` now rejects flags outside CE's `ILCF_VALID` mask, rejects CE-unsupported cross-list copies, copies only between valid slots in the same image list for `ILCF_MOVE`, and swaps same-list slots for `ILCF_SWAP`. `ImageList_Draw` now uses CE's `CLR_DEFAULT` background wrapper default, and `ImageList_DrawIndirect` now enforces the exact CE `IMAGELISTDRAWPARAMS` size before recording or rendering a draw, resolves `CLR_DEFAULT` backgrounds to the image-list background color, defaults zero `cx`/`cy` after applying `xBitmap`/`yBitmap`, writes those normalized draw parameters and CE's final overlay-pass `i`/`x`/`y`/`cx`/`cy`/`fStyle` mutations back into the caller's `IMAGELISTDRAWPARAMS`, recognizes CE's private `ILD_BLENDMASK` shape so `ILD_BLEND75` enters the bitmap-backed blend path instead of drawing unblended pixels, supports CE `rgbFg == CLR_NONE` destination blending for bitmap-backed color-image draw targets, follows CE's mask-only `rgbFg == CLR_NONE` blend branch by forcing `ILD_TRANSPARENT` and applying the 50% mono-dither/SRCAND mask path instead of destination tinting, applies CE `ILD_ROP`/`dwRop` raster operations for `ILD_MASK` and `ILD_IMAGE` selected-DIB/framebuffer draws, and still draws registered overlays when `ILD_MASK` is combined with `ILD_OVERLAYMASK`. `ImageList_CopyDitherImage` now preserves destination image metadata like CE, masks `fStyle` to overlay bits, mutates bitmap-backed destination image pixels, and applies CE's 50% mono-dither/SRCAND mask update when destination and source mask storage is available. `ImageList_Duplicate` now deep-copies bitmap-backed image/mask storage like CE `CopyDIBBitmap`/`CopyBitmap` while preserving list metadata, overlays, and pseudo-handle entries. `ImageList_Remove`/`ImageList_SetImageCount` now preserve CE overlay slot indexes for single-image removals and truncation while only `ImageList_Remove(-1)` clears overlays and other negative remove indexes fail, and `ImageList_SetImageCount` now preserves the existing count and returns failure when the backing allocation request cannot be satisfied. `ImageList_SetOverlayImage` now rejects image lists created without `ILC_MASK` like CE's missing-mask-DC path, enforces CE's four overlay slots, computes CE-style overlay mask bounds/`ILD_IMAGE` metadata for drawing, covers CE's all-white-mask branch where the overlay record is zero-sized and paints no overlay pixels, preserves existing bounds when the same overlay slot is set to the same image like CE's early return, and keeps non-rectangular overlay masks mask-driven instead of promoting them to `ILD_IMAGE`; `ImageList_SetIconSize` now rejects no-op size changes while clearing all images/overlays on real size changes, including CE's changed zero/negative dimension case, `ImageList_BeginDrag` now creates the drag image hidden like CE's `s_DragContext.bDragShow = false` path, `ImageList_DragMove` now returns success even when no drag image is active while only moving visible drags, `ImageList_SetDragCursorImage` now treats a missing drag/dither image as a successful no-op, and `ImageList_GetDragImage`/`ImageList_DragEnter`/`ImageList_DragLeave` now preserve CE static drag point, hotspot, visibility, and lock state even when no drag image is active. Remaining gaps are broader mask-only format variants, image-list palette variants, additional overlay rendering edge cases, and additional cleanup/error combinations.

- `ImageList_Destroy` now releases image-list-owned bitmap/mask backing storage and heap allocations like CE `ImageList::Cleanup` deleting `m_hbmImage`/`m_hbmMask`; raw `ImageList_Replace`, `ImageList_ReplaceIcon`, `ImageList_Remove`, `ImageList_SetIconSize`, and `ImageList_SetImageCount` now also release cloned bitmap backing handles once no remaining list entry references them, while preserving shared strip handles that still have live entries. Raw `ImageList_SetBkColor` now applies CE `ResetBkColor`-style backing-pixel updates for bitmap-backed masked image lists, rewriting mask-on slots to black/white/the selected background while leaving mask-off source pixels unchanged. Raw `ImageList_GetIcon` now follows CE's mask/color draw pass for bitmap-backed entries with real backing storage by returning a real icon with rendered color and mask bitmaps instead of a bitmap-derived pseudo handle; local icon objects now track owned bitmap backing so `DestroyIcon` releases `ImageList_GetIcon`, PE-extracted, and copied `CreateIconIndirect` backing storage without deleting caller-owned source bitmaps. Synthetic shell/system image-list icons now ignore invalid overlay-mask slots above CE's four `ImageList_SetOverlayImage` entries for both returned pseudo handles and pseudo rendering, and draw their pseudo body/overlay markers into selected memory DIBs as well as framebuffers. Remaining cleanup/rendering work is narrower synthetic pseudo-icon rendering edge validation plus broader palette/dither cases.

### GWE, Menu, And Modal Message Gaps

Reference file:
- `C:\WINCE600\PUBLIC\COMMON\SDK\INC\winuser.h`

Missing or under-validated:
- `TrackPopupMenuEx` now covers the core CE popup loop shape, including `TPM_NONOTIFY`, `TPM_RETURNCMD`, disabled/default items, submenu cascades, owner notifications, cancellation behavior, CE horizontal/vertical alignment flags, and top-level `TPMPARAMS.rcExclude` screen-rectangle avoidance with below-placement, right-side fallback after vertical candidates collide, left-side fallback when the right candidate clamps back into the excluded region, and the final first-clamped-candidate fallback when every placement still intersects the exclusion. Remaining gaps are broader live nested modal routing and additional unusual edge placement combinations.
- `MessageBoxW` now validates CE-supported button, default-button, icon, `MB_SETFOREGROUND`, `MB_TOPMOST`, and `MB_RTLREADING` flag shapes while rejecting unsupported desktop-only or undefined bits; it also covers default return IDs, yes/no/cancel layouts, owner-window disabled state, queued modal input, transient active-dialog recording, and `MB_TOPMOST` creation with the CE `WS_EX_TOPMOST` extended style. Remaining gaps are deeper modality, timeout/pump behavior, foreground/topmost z-order effects in live windows, and broader live user-driven dispatch.
- `SendMessageTimeout` currently covers important paths, including unsupported flag rejection, zero-timeout cross-thread expiry, Unicorn block-prep refusal for zero-timeout cross-thread sends so the raw expiry path owns that CE case, nonzero cross-thread queueing, nonzero timeout expiry without stale receiver delivery, CE-public `SMTO_NORMAL` nonzero cross-thread completion, destroyed-target completion, cross-thread `SMTO_ABORTIFHUNG` queuing just below the CE hung threshold, aborting once the receiver is hung, and aborting the combined `SMTO_BLOCK | SMTO_ABORTIFHUNG` path once the receiver is hung, same-thread synchronous dispatch/result writes, same-thread `SMTO_ABORTIFHUNG` dispatch despite a hung-threshold timestamp, early `ReplyMessage` result propagation, nested timeout sends where an inner `ReplyMessage` completes without clearing the outer send, and the CE `MessageTimeout::cReference`-style case where an active outer send times out while a nested send still dispatches and completes independently. Sent-message retrieval now honors caller HWND/min/max filters so queued notify sends and mount `WM_DEVICECHANGE` broadcasts do not satisfy unrelated `GetMessageW`/`PeekMessageW` filters. Runtime process handoff now clears stale cross-process send-yield snapshots when the corresponding live GWE sent-message record is gone, keeping remote/monitor rotation tied to the current CE send queue. Remaining gaps are broader reentrant dispatch, additional cross-thread `SMTO_BLOCK`/`SMTO_ABORTIFHUNG` edge combinations used by CE callers, and a fresh driven iNavi run to validate the repaired handoff in context.
- `MsgWaitForMultipleObjectsEx` now covers CE's ignored desktop `MWMO_WAITALL` bit, `QS_TIMER`, `QS_SENDMESSAGE`, `QS_ALLINPUT` over posted messages, paint, and timers, `MWMO_INPUTAVAILABLE` new-vs-existing input behavior, signaled-handle precedence over message wake, message-wake return indexing after multiple unsignaled handles, and internal handle probing without double-counting the wait as a separate `WaitForMultipleObjects` scheduler attempt. Remaining gaps are cancellation edges, additional live multi-handle waits, and live modal-loop interaction.

### File Notification And FSDMGR Gaps

Reference files:
- `C:\WINCE600\PRIVATE\WINCEOS\INC\fsnotify_api.h`
- `C:\WINCE600\PRIVATE\WINCEOS\COREOS\STORAGE\FSDMGR\pathapi.cpp`
- `C:\WINCE600\PRIVATE\WINCEOS\COREOS\STORAGE\FSDMGR\fsdmgrapi.cpp`
- `C:\WINCE600\PRIVATE\WINCEOS\COREOS\STORAGE\FSDMGR\fsdcache.cpp`
- `C:\WINCE600\PRIVATE\WINCEOS\COREOS\STORAGE\FSDMGR\nullcache.cpp`
- `C:\WINCE600\PRIVATE\WINCEOS\COREOS\STORAGE\FSDMGR\mounttable.cpp`
- `C:\WINCE600\PUBLIC\COMMON\OAK\INC\diskio.h`
- `C:\WINCE600\PRIVATE\WINCEOS\COREOS\STORAGE\NOTIFY\fsnotify.cpp`

Missing or under-validated:
- CE distinguishes external caller handles from internal/kernel-owned handles through `FSEXT_*`, `FSINT_*`, `NotifyGetNextChange`, and `INT_NotifyGetNextChange`; `FindCloseChangeNotification` now consumes valid-but-wrong handle types before reporting `ERROR_INVALID_HANDLE`, matching the public `DUPLICATE_CLOSE_SOURCE` path, while `FSINT_FindCloseChangeNotification` now rejects valid-but-wrong handles without consuming them like CE `INT_NotifyCloseChangeHandle`. Raw `DuplicateHandle` now validates source/target/options while creating independent local handle-table entries so duplicated notification handles survive `DUPLICATE_CLOSE_SOURCE`, duplicated notification handles are owned by the requested target process, public file-change notification handles now record the creating process and reject foreign-process wait/reset/info/duplicate/direct-`CloseHandle`/find-close attempts without consuming the handle, and direct `AFS_FindFirstChangeNotificationW` now honors its nonzero `hProc` owner, including foreign-caller direct `CloseHandle`/`DuplicateHandle` rejection without consuming the target-owned notification or publishing a duplicate. `fsdmgr.dll` imports now recognize the CE `fsdmgr.def` notification ordinals 68-75, paired `FSINT_*`/`FSEXT_*` names, `FSDMGR_CacheIoControl @3`, `FSDMGR_CachedRead @4`, `FSDMGR_CachedWrite @5`, `FSDMGR_CreateCache @6`, `FSDMGR_CreateFileHandle @7`, `FSDMGR_CreateSearchHandle @8`, `FSDMGR_DeleteCache @9`, `FSDMGR_DeregisterVolume @10`, `FSDMGR_DiskIoControl @12`, `FSDMGR_FlushCache @14`, `FSDMGR_GetVolumeHandle @21`, `FSDMGR_GetVolumeName @22`, `FSDMGR_InvalidateCache @24`, `FSDMGR_ReadDisk @25`, `FSDMGR_ReadDiskEx @26`, `FSDMGR_RegisterVolume @27`, `FSDMGR_ResizeCache @30`, `FSDMGR_SyncCache @32`, `FSDMGR_WriteDisk @35`, `FSDMGR_WriteDiskEx @36`, `FSDMGR_GetMountFlags @37`, and `STOREMGR_FsIoControlW @44`; `FSEXT_*` creates caller-owned first-change handles, while `FSINT_*` now creates internal FSDMGR-owned notification handles whose reset/info/close path works only through the paired internal imports and rejects public/`FSEXT` access without touching caller buffers. Raw AFS volume handles now record their owning process, reject foreign `AFS_Unmount` and `AFS_FsIoControlW`, close/unmount through `AFS_Unmount` or direct `CloseHandle`, consume the volume handle after the first close, signal root plus subpath file-change watchers through the mounted-root removal path, report mounted `CE_VOLUME_INFO` for `AFS_FsIoControlW(FSCTL_GET_VOLUME_INFO)` and the FSDMGR `STOREMGR_FsIoControlW` import, expose mounted volume folder names plus CE AFS hidden/system/permanent mount flags through FSDMGR import traps, map FSD `PDSK` tokens to HVOLs through the FSDMGR register/get-volume-handle support path, return FSD file/search context pointers through the CE FSDMGR create-handle shims, model the CE null-cache fallback ID table plus success/invalid/not-supported cache return surface, cover `STOREMGR_FsIoControlW` refresh/flush no-ops plus host-backed unsupported-FSCTL no-touch failure, validate `FSCTL_COPY_EXTERNAL_START`/`COMPLETE` fixed `FILE_COPY_EXTERNAL` payloads before host-backed unsupported no-touch failure, cover file-handle `FSCTL_SET_FILE_CACHE(FileCacheDisableStandard)` no-op success plus unsupported enable/discard and volume-level rejection, and route direct/cache/Ex disk reads and writes through sparse synthetic sectors with CE `DISK_INFO`, `GETNAME`, `DEVICE_INFO`, `GET_STORAGEID`, `INITIALIZED`, `FORMAT_MEDIA`, `GET_SECTOR_ADDR` validation with no-XIP unsupported failure, `COPY_EXTERNAL_START` `DISK_COPY_EXTERNAL` payload validation with unsupported no-touch failure, `GETPMTIMINGS` synthetic zero-timing output, `SECURE_WIPE` sparse-sector clearing, `SET_SECURE_WIPE_FLAG` payload validation/no-op behavior, `DELETE_SECTOR_INFO`-backed sector clearing, and flush-cache IOCTL results. Remaining work should validate physical block-driver backing, broader external cache DLL/filter behavior, remaining specialized disk IOCTLs, real external-copy accelerator behavior, and real mounted-FSD `FsIoControl` hook forwarding beyond the host-backed unsupported stub.
- `FindFirstChangeNotificationW` now canonicalizes unrooted, `.`, and `..` watch paths before directory validation and watch registration, matching the CE `SafeGetCanonicalPathW` path into volume resolution, and preserves caller notification filter bits instead of rejecting unknown flags before the CE notify matcher gates on known bits. Root and non-root watches now honor the CE `WatchSubtree` boundary: non-recursive watches see only immediate children, while recursive watches see deeper descendants. `MoveFileW` file-change records now follow CE `NotifyMoveFileEx` boundaries, reporting same-parent moves as rename old/new and cross-parent moves as remove/add with file-vs-directory notify filters, and directory watchers now see self-rename/removal as CE current-directory removed (`"\\"`). Mounted change-notification handles now retain the owning mount root like CE `NOTVOLENTRY`-scoped events, so same child paths on different mounted volumes do not wake each other, same-parent mounted renames return old/new records only to the owning volume while recursive root watches see mounted-volume-prefixed old/new paths, cross-parent mounted renames return remove/add records to the owning volume while sibling mounted volumes stay quiet, and recursive root watchers still see mounted-volume-prefixed create/delete and rename paths. Remaining coverage should expand into less common notification lifetime/reset edges.
- `CeGetFileNotificationInfo` now follows the CE `FILE_NOTIFY_CHANGE_CEGETINFO` detail-record gate for signal-only vs detailed watches, follows CE `NotifyReset` buffer/reset behavior for prefix drains, re-signaling, no-more-items, undersized buffers, pending no-fit output-count write order, fitted-record count-pointer fault draining, partial caller-buffer fault copied-prefix draining, no-pending null-returned pointer ordering, the all-zero no-data reset path that consumes one outstanding signal, null buffers whose nonzero length reaches the guarded write path, no-pending guarded output writes with `ERROR_NO_MORE_ITEMS`, and trailing-NUL `FILE_NOTIFY_INFORMATION` record sizing/copying while preserving non-NUL `FileNameLength`, and `FindNextChangeNotification` now consumes one outstanding notification signal instead of clearing every queued detailed record. Successful writes still report `FILE_ACTION_MODIFIED`, and closing a changed file handle now reports CE's `FILE_ACTION_CHANGE_COMPLETED` detailed record with the FSDMGR `NotifyCloseHandle` filter mask. Access-denied read-only mount mutations now also leave matching watches unsignaled with no detailed records, and foreign-process notification handles now fail before draining records. Remaining gaps are broader error propagation and more exhaustive buffer/reset edge coverage.
- FSDMGR move behavior now covers mounted storage boundaries for cross-volume file moves, cross-volume directory rejection, mount-point rename errors, CE same-parent vs cross-parent notification action mapping, watched-directory self-rename/removal records, same-parent mounted rename scoping, and cross-parent mounted rename remove/add scoping across owning-volume, sibling-volume, and recursive-root change handles. `DeleteAndRenameFile`/`AFS_PrestoChangoFileName` now follows CE's delete-old/move-new direction, same-volume restriction, destination-preserving failure behavior, and delete-plus-move notification sequence. Cross-volume file moves now follow CE `FS_MoveFileW` by copying to the destination and treating source deletion as best-effort, so moves off read-only media can succeed while leaving the source file and only signaling the destination create. Mounted-root directory create/remove, file-delete, and attribute changes now follow CE `pathapi.cpp` errors, and read-only mounted host roots now report access-denied for raw create/copy/delete/directory/same-volume rename/attribute mutation attempts without mutating host storage or queuing change notifications. System, hidden, read-only, removable, and permanent mount metadata now follows CE `virtroot.cpp`, `pathapi.cpp`, `volumeapi.cpp`, `storemgr.h`, `fsdmgrapi.cpp`, and `pwindbas.h`: root enumeration skips hidden mount folders, exact hidden mount probes report `FILE_ATTRIBUTE_HIDDEN`, system mount roots and nested entries report `FILE_ATTRIBUTE_SYSTEM`, `CeGetVolumeInfoW`/`CeFsIoControlW(FSCTL_GET_VOLUME_INFO)`/volume-handle `AFS_FsIoControlW(FSCTL_GET_VOLUME_INFO)`/FSDMGR `STOREMGR_FsIoControlW(FSCTL_GET_VOLUME_INFO)` expose system/hidden/read-only/removable volume attributes, FSDMGR `GetVolumeName`/`GetMountFlags` import traps return mounted folder names plus AFS hidden/system/permanent flags, FSDMGR `RegisterVolume`/`GetVolumeHandle` import traps associate FSD disk pointers with registered HVOLs, FSDMGR `CreateFileHandle`/`CreateSearchHandle` import traps return the FSD-owned context pointers as CE does, FSDMGR `STOREMGR_FsIoControlW` now covers refresh/flush no-op, copy-external fixed-payload validation/unsupported no-touch, and unsupported host-backed no-touch failure edges, file-handle `DeviceIoControl(FSCTL_SET_FILE_CACHE)` now follows the CE cache-filter disable-only surface, and FSDMGR cache import traps now follow the CE null-cache fallback for create/delete/resize/flush/sync/invalidate/ioctl status behavior while direct/cache/Ex sector reads and writes persist sparse synthetic disk sectors and `FSDMGR_DiskIoControl` handles CE `DISK_INFO`, metadata/status IOCTLs, SG read/write, `GET_SECTOR_ADDR` validation/no-XIP unsupported behavior, `COPY_EXTERNAL_START` `DISK_COPY_EXTERNAL` payload validation/unsupported no-touch behavior, `GETPMTIMINGS` zero timing snapshots, secure-wipe sector clearing and set-flag validation/no-op behavior, `DELETE_SECTOR_INFO` sector clearing, format-media clearing, and flush-cache basics. Remaining volume-handle paths should be tested for physical block-driver backing, broader external cache DLL/filter behavior, remaining specialized disk IOCTLs, real external-copy accelerator behavior, and real mounted-FSD `FsIoControl` hook forwarding beyond the host-backed unsupported stub.

### Loader And Runtime Gaps

Reference evidence:
- `C:\WINCE600\PRIVATE\WINCEOS\COREOS\NK\TOOLS\ROMIMAGE\ROMIMAGE\pbtimebomb.cpp`
- `C:\WINCE600\PRIVATE\WINCEOS\COREOS\NK\LOGGER\celoginstrumentation.c`
- `C:\WINCE600\PRIVATE\WINCEOS\COREOS\CORE\DLL\loader.cpp`
- `C:\WINCE600\PUBLIC\OSTEST\CESYSGEN\makefile`

Missing or under-validated:
- `LoadLibraryEx` now validates CE resource-only combinations such as `LOAD_LIBRARY_AS_DATAFILE | DONT_RESOLVE_DLL_REFERENCES`, rejects non-null `hFile` handles with `ERROR_INVALID_PARAMETER`, and promotes an already loaded `DONT_RESOLVE_DLL_REFERENCES` module to normal loaded-module flags when a later raw/normal load requests it without no-import flags. Keep expanding cases around resource-only handle behavior as more CE source consumers are found.
- Loader lifecycle now covers CE `ThreadNotifyDLLs`-style `DLL_THREAD_ATTACH`/`DLL_THREAD_DETACH` entrypoint callouts for imported modules that are not loading, no-import, or marked by `DisableThreadLibraryCalls`, and runtime `ProcessDetachAllDLLs` now follows CE `ProcessDetach` min-refcount ordering before releasing per-process module refs. Raw `ProcessDetachAllDLLs` cannot enter guest callouts, but it now drains eligible loaded-module refs while preserving no-import/datafile modules. Remaining lifecycle gaps are build-entry details around `DllEntry`, `_DllMainCRTStartup`, C++ static constructors/destructors, and broader process/thread ordering fixtures.
- Runtime DLL mapping parity should be audited outside Unicorn so raw loader tests and Unicorn execution do not silently diverge.
- Forwarder resolution now includes a host fixture where a named export forwards to a target DLL ordinal, and malformed literal forwarder strings fail closed instead of being whitespace-normalized into valid targets. Runtime `LoadLibraryW` failures now release modules newly loaded during the failed attempt, matching CE `DoImports` cleanup of dependency blocks, final dynamic `FreeLibrary` now releases the module's dependency refs after detach planning, failed runtime DLL maps now unmap their current image reservation before returning failure, current-image resource/trap/trampoline/module state is committed only after fallible parse/forwarder/resource steps succeed, and `DllMain(DLL_PROCESS_ATTACH)` false returns now set `ERROR_DLL_INIT_FAILED` while releasing refs owned by the failed load attempt. Remaining gaps are deeper lifecycle failure variants and broader integration fixtures for loader failure paths.

### Winsock And Network Gaps

Reference evidence:
- `C:\WINCE600\PRIVATE\TEST\NET\WINSOCK\22`
- `C:\WINCE600\PRIVATE\SERVERS\WEBPROXY\CORE\session.cpp`
- `C:\WINCE600\PRIVATE\WINCEOS\COREOS\DEVICE\SERVICES\CORE\servacpt.cpp`

Missing or under-validated:
- Local Winsock already has core socket calls, `ioctlsocket`, `setsockopt`, `getsockopt`, and `select`; `select` now accepts CE caller `nfds` values of `0`, `-1`, and active counts while rejecting null fd-set triads, `fd_count > FD_SETSIZE`, invalid socket handles, and fd-set memory faults with Winsock errors. Mixed read/write/except set filtering, zero-ready recovery, graceful TCP peer-close read readiness, TCP half-close read/write behavior, TCP reset read readiness/error propagation, repeated listener `select`/`accept` loops, and `SO_ERROR` clearing after `getsockopt` are covered. Remaining work is broader CE stress parity across higher-volume socket churn and full webproxy/service accept-loop patterns.
- Add more tests based on CE server/stress patterns for higher-volume socket churn, concurrent accept/send/close transitions, and webproxy/service session lifetimes.

### Input, IME, Text, And GDI Gaps

Reference evidence:
- `C:\WINCE600\PUBLIC\WCESHELLFE\OAK\INPUTMETHODS`
- `C:\WINCE600\PUBLIC\COMMON\SDK\SAMPLES\TESTIME`
- `C:\WINCE600\PUBLIC\COMMON\SDK\SAMPLES\MULTIBOX`
- `C:\WINCE600\PUBLIC\COMMON\SDK\SAMPLES\CHTIM`
- `C:\WINCE600\PRIVATE\TEST\STRESS\STRESS\MODULES\FONT`
- `C:\WINCE600\PRIVATE\TEST\STRESS\STRESS\MODULES\DIB`
- `C:\WINCE600\PRIVATE\TEST\STRESS\STRESS\MODULES\ALPHA`

Missing or under-validated:
- IME-facing input paths now cover `ImmGetOpenStatus(NULL)`, `ImmSetOpenStatus(NULL, ...)`, `ImmGetConversionStatus(NULL, ...)`, `ImmSetConversionStatus(NULL, ...)`, `ImmGetCompositionStringW(NULL, GCS_COMPSTR, ...)`, `ImmSetCompositionStringW(NULL, SCS_SETSTR, ...)`, `ImmGet/SetCompositionWindow`, `ImmGet/SetCompositionFontW`, `ImmGet/SetCandidateWindow`, `ImmGet/SetStatusWindowPos`, `RegisterSIPanel`, `SHSipPreferenceI`, `ImmSIPanelState(SIP_QUERY_LOCATION/SIP_SET_LOCATION/SIP_INPUT_ATTRIBUTES)`, `ImmGetDefaultIMEWnd`/`DefaultImeWndGet` focused-window and caller-HWND proxy behavior, `ImmIsUIMessageW` forwarding for the CE `WM_IME_*` UI message family, `ImmGetHotKey` no-registered-hotkey output clearing, `ImmGetGuideLineW` no-guideline level/string/private-data query behavior plus HIMCC-backed `GUIDELINE` level/index/string/private payload reads through `hGuideLine`, `ImmLockIMC`/`ImmUnlockIMC` and `ImmLockIMCC`/`ImmUnlockIMCC` guest-readable `INPUTCONTEXT`, `COMPOSITIONSTRING`, and `CANDIDATEINFO` buffers with lock-count/size queries, `ImmUnlockIMC` readback of resized `hCompStr`/`hCandInfo` `COMPOSITIONSTRING`/`CANDIDATEINFO` payload mutations, `ImmGetCandidateListCountW`/`ImmGetCandidateListW` CE `CANDIDATELIST` payload marshaling for stored HIMC candidate lists, CE-shaped `ImmNotifyIME` candidate open/change/close plus candidate selection/page, composition-font, and status-window notifications, TESTIME/`imm.h`-backed `ImmGetProperty` capability values, TESTIME resource-backed `ImmGetIMEFileNameW`/`ImmGetDescriptionW` strings for IME HKLs, TESTIME zero-result `ImmGetConversionListW`, TESTIME `ImmEscapeW(IME_ESC_QUERY_SUPPORT)` self-query behavior, CE keyboard-layout transition APIs including loaded HKL lists, `LoadKeyboardLayoutW` `KLF_ACTIVATE`, `ActivateKeyboardLayout` `HKL_NEXT`/`HKL_PREV` cycling, TESTIME `dic.c`-backed single-letter candidate generation for synthetic IME HKLs, TESTIME `regword.c` fake-word style/register/unregister state plus registered-word candidate inclusion and `ImmEnumRegisterWordW` callback enumeration, and TESTIME `GenerateMessage`/`hMsgBuf` `ImmGenerateMessage` queue flushing, resolving active-HIMC calls through the current foreground/focused keyboard target's HIMC where the CE input-method samples do so. Remaining coverage is still needed for rendered guideline/candidate UI callbacks, broader dictionary/private-profile candidate sources, and broader IME handoff.
- TESTIME candidate-list generation now also rejects combined built-in/registered-word/private-profile lists above the CE `MAXCANDSTRNUM` ceiling of 32, matching `dic.c::ConvKanji`; broader rendered candidate UI and IME handoff remain open.
- TESTIME registered-word candidate lookup and Unicorn `ImmEnumRegisterWordW` callback enumeration now follow `stub.c::GetPrivateProfileString` by hiding private-profile entries for readings with lowercase characters while preserving uppercase readings.
- TESTIME candidate generation now also reads registry-value private-profile dictionary entries under `HKLM\SOFTWARE\Microsoft\testime\Windows\testime.DIC\<reading>` after the built-in table, preserving string data as the candidate text when present. Kernel boot now seeds the CE sample `testime.reg` dictionary into that active value-enumeration shape, including the numeric rolling candidates.
- Text and font behavior now includes selected-font `GetTextFaceW` parameter parity for null output buffers and negative counts, plus CE `GetTextExtentExPointW` invalid-parameter parity for null output `SIZE`, null input text with a positive count, and negative character counts. Device-context state coverage now includes CE `GetBkMode`, `SetBkMode`, `GetBkColor`, `SetBkColor`, `GetTextColor`, `SetTextColor`, `GetTextAlign`, and `SetTextAlign` invalid-HDC last-error behavior, `CLR_INVALID` background/text color round-trip error behavior, CE sentinel/validation behavior for `Get/SetStretchBltMode`, `Get/SetTextCharacterExtra`, and `Get/SetLayout`, CE invalid-HDC behavior for exported `SetViewportOrgEx`, CE `SetBrushOrgEx` invalid-HDC and previous-origin behavior, and CE `SaveDC`/`RestoreDC` invalid-HDC, level-zero, negative-level, and absolute-level restoration behavior. Raw `GetTextExtentExPointW`, `DrawTextW`, and `ExtTextOutW` now apply CE `SetTextCharacterExtra` advance math for positive spacing, CE's first-character-preserving negative-spacing rule, and caller-provided `ExtTextOutW` `lpDx` advances, while selected CE Arial raster fonts now reject `SetTextCharacterExtra` without mutating the existing DC spacing value. Raw `GetCharABCWidthsI` now follows the CE `wingdi.h` glyph-index/count signature instead of the non-`I` first/last range shape, and plain 16px selected Tahoma `GetCharABCWidths`/`GetCharABCWidthsI` now use the CE GDIAPI `fontdata.h` ABC table for `!` through `z`. The stock/fallback `Tahoma` `TEXTMETRICW` path now carries the CE GDIAPI `fontdata.h` Tahoma `tmPitchAndFamily` and `tmCharSet` bytes, and plain 20px selected-font metrics now follow the CE GDIAPI `NTFontMetrics` rows for Tahoma, Courier New, Symbol, Times New Roman, Wingdings, and Verdana for the `TEXTMETRICW` fields the fixture compares. Plain 16px selected-font extents now use the CE GDIAPI `NTExtentResults` width tables for Tahoma, Courier New, Times New Roman, Wingdings, Verdana, and the Arial raster face across `GetTextExtentExPointW`, `GetCharWidth32`, and shared text-run measurement. Raw `DeleteObject` now preserves selected GDI object lifetime by failing while a font/brush/pen/bitmap/palette remains selected into any live DC, and selected-font metrics remain active until the DC selects another font. Raw `ExtTextOutW` also honors `OPAQUE` background mode for selected memory DIBs and framebuffers by filling text cells with the DC background color before drawing glyph pixels, clips glyph and opaque text-cell output through both the optional `ETO_CLIPPED` rectangle and the selected DC clip region, and validates null/bad HDCs before null text pointers to match CE `passNull2Text` last-error ordering. Broader CE-style glyph metrics outside the fixture ranges, additional font-size/style selection/fallback, complex text clipping, and additional font lifetime checks remain.
- Raw zeroed `LOGFONTW` `CreateFontIndirectW` now follows CE `font.cpp::TestCreateFontIndirectZero`: after selection, `GetCurrentObject(OBJ_FONT)` returns the zero-font handle and `GetObjectW` returns an all-zero `LOGFONTW` instead of normalizing to fallback font fields.
- Raw `GetTextMetricsW` now follows CE `font.cpp::passOddSize` realized-height rows for plain known-font `CreateFontIndirectW` selections with `lfHeight` 0 and -24 across Tahoma, Courier New, Symbol, Times New Roman, and Wingdings.
- Raw `CreateFontIndirectW`/`GetObjectW` now preserves nonzero `LOGFONTW` escapement, orientation, precision, quality, pitch/family, style, charset, and face-name fields instead of dropping them during font object serialization.
- Raw `GetCharABCWidths` now follows CE `font.cpp::abcEscapementTest` by rejecting selected TrueType-style fonts with nonzero escapement and setting `ERROR_INVALID_PARAMETER`.
- Bitmap and alpha paths now include CE `BitBlt`/`StretchBlt` invalid-HDC and invalid ROP4 rejection, selected-DIB/framebuffer rendering for common CE ROP3 operations (`SRCPAINT`, `SRCAND`, `SRCINVERT`, `SRCERASE`, `MERGEPAINT`, `NOTSRCCOPY`, `NOTSRCERASE`, `DSTINVERT`), selected-brush pattern ROPs (`MERGECOPY`, `PATCOPY`, `PATINVERT`, `PATPAINT`), and generic CE ROP3 truth-table evaluation for the literal `draw.cpp::gnvRop3Array` cases across `BitBlt`/`StretchBlt`, CE `draw.cpp::NegativeSize` signed destination extent mirroring for selected-DIB source-copy `BitBlt` and null-mask `MaskBlt`, and CE `StretchBlt` signed destination/source extent mirroring for selected-DIB draws. Raw `CreateDIBPatternBrushPt` now validates null/unsupported inputs like CE `brush.cpp`, creates a private bitmap from packed DIB data, and tiles through the existing pattern-brush renderer. Raw direct-DIB paths now render `StretchDIBits` and `SetDIBitsToDevice` into selected memory DIBs, decode CE GDIPRINT-style indexed 2 bpp `DIB_RGB_COLORS` caller DIBs, accept `DIB_PAL_COLORS` palette-index tables for direct indexed DIB sources, validate CE bad-HDC/ROP4 edges for `StretchDIBits`, and convert bottom-up `SetDIBitsToDevice` source scanline arguments like CE `SimpleSetDIBitsToDeviceTest`. CE `MaskBlt` parameter validation for null/bad destination and source DCs, bad/color mask bitmaps, negative mask origins, and out-of-bounds 1bpp mask rectangles is covered, plus selected-DIB and framebuffer `MaskBlt` rendering for the CE `draw.cpp` `MAKEROP4(DSTCOPY, SRCCOPY)` mask-copy path, generic foreground/background ROP3-byte evaluation from CE `TestAllRops`-style ROP4 values, and CE `swblt.cpp` negative destination extent mirroring for masked selected-DIB/framebuffer draws. Raw `AlphaBlend` now follows CE invalid-HDC and blend-function validation for `BlendOp`, unsupported `AlphaFormat`, and 32bpp-only per-pixel alpha while accepting the CE GPE `BLT_ALPHASRCNEG`/`BLT_ALPHADESTNEG` blend flags, accepts zero source or destination dimensions as successful no-op rectangles like CE `AlphaBlendGoodRectTest`, rejects negative dimensions and selected-DIB source rectangles outside the bitmap bounds like CE `AlphaBlendBadRectTest`, covers selected-DIB source-constant-alpha blending, applies CE premultiplied `AC_SRC_ALPHA` and non-premultiplied `AC_SRC_ALPHA_NONPREMULT` paths, applies CE `BLT_ALPHASRCNEG` source-alpha/source-constant inversion to selected-DIB and framebuffer destinations, applies CE `BLT_ALPHADESTNEG` destination-alpha inversion to 32bpp selected-DIB and alpha-capable framebuffer destinations, applies top-down and bottom-up 32bpp per-pixel alpha to selected-DIB destinations, applies source-constant plus top-down and bottom-up 32bpp per-pixel alpha to framebuffer destinations, clips negative selected-DIB and framebuffer destination origins while preserving CE stretch source mapping, and now uses CE GPE `swblt.cpp` Bresenham stretch sampling for uneven selected-DIB and framebuffer alpha stretches instead of simple floor-division sampling. Remaining gaps are broader DIB format/orientation variants, palette state beyond direct source palette indexes, additional MaskBlt ROP4 clipping/mirroring cases, and additional alpha orientation/clipping edge cases.
